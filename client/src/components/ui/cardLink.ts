/** The list-screen card: a white panel on the cream canvas that presses in when tapped.
 * Shared by the story, word-topic and word grids so the three stay in step. */
export const CARD_LINK =
  'flex flex-col items-center gap-2 rounded-xl3 bg-white p-6 shadow-card transition-transform active:scale-95'
