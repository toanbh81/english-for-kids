# Vòng 3 "Danh sách và điều hướng" → brief triển khai (2026-09-03)

Nguồn: `docs/design/round-2026-09/Speak Up Lists & Nav.dc.html` (88 KB; 4 mục, 13 artboard — C6 ×3 frame, C1, C8, A3 ×2, A6 ×2, A7, A8, A9 ×2, C2, C3 ×2 — cộng catalog ô 8 mẫu, thẻ MissionCard 4 trạng thái và 4 thẻ Q11–Q14).
Đích: `client/` sau Phase 12 (khung trang) và Phase 13 (khung luyện nói). Phase triển khai: **Phase 14**.

> **Cách đọc.** Số đo trích **nguyên văn** từ inline style / `renderVals()`. Chỗ design không có số ghi "design không ghi" + đề xuất đánh dấu *(đề xuất)*. Mã màn theo inventory (A3–A17, B2, C1, C5, C6, C8, C2, C3). Phase 12 đã dựng `PageShell/PageHeader/PageBody/PageFooter`, `Button md/lg/adult`, `Stars`, `Notice/NoticeStack`, `EmptyState`, `NotFound`, `LessonChip` trong ô phải của header; Phase 13 thêm `ChipPair`, `PageHeader.dimmed`, `PageBody.split`, variant `short:` và luật `ipad:` thắng `md:`. Vòng này **không** đụng 9 màn luyện nói — nó dựng **khung danh sách** và sửa **điều hướng**.

---

## 0. Bốn quyết định gốc của design

1. **Một khung danh sách, hai cỡ ô + một cỡ hàng.** Thẻ mở đầu: "**Ô nhỏ** (từ, âm, truyện, chủ đề — nội dung ≤ 1 từ): **3 / 5 / 6 cột** (phone / iPad dọc / iPad ngang), cao 110/136, emoji 40/56." · "**Ô lớn** (câu, cặp từ, đoạn — có 2 dòng chữ): **2 / 3 / 4 cột**, cao 128/160, chữ 17/20px, tối đa 2 dòng ellipsis." · "**Hàng** (SentenceList, StoryList phone): cao 64/72, text trái + sao phải; iPad tách 2 cột hàng." · "Danh sách dài (64 ô, 32 hàng) nhóm theo chủ đề với **H2 dính** 15px. Empty & not-found dùng component Vòng 1." Catalog ghi cỡ thật: ô nhỏ **114×110**, ô lớn **171×128**, "cỡ phone; iPad ×1.25".
2. **Header một hàng, căn trái.** "Header 1 hàng: Back · H1 22/28px + phụ 13/15px **căn trái** · ô phải (LessonChip / ⭐ tổng sao / trống). **Không còn H1 40px căn giữa**." Phone: Back 56 tròn, H1 22 + phụ 13, ô phải 56×56; iPad (cả 2 hướng): Back 64, H1 28 + phụ 15, ô phải pill cao 48.
3. **Điều hướng — banner và copy.** "**Banner Home**: tối đa 2 hiện, ưu tiên lỗi → cảnh báo → info; cái thứ 3 gộp thành dòng "+1 thông báo ▸"." · "**Copy "Về bản đồ"** chỉ khi Home là bản đồ (iPad ngang). Phone & iPad dọc: "Về trang chủ 🏠"."
4. **CTA ghim đáy thống nhất + header StoryPlayer lên trên tranh.** "**StoryPlayer**: header chuẩn (Back · chip "Cảnh 2/7" · LessonChip) **trên** tranh, không overlay trên tranh → hết đè." · "**CTA ghim đáy** thống nhất: TopicHub "Học tiếp: …", LevelStairs "Luyện bậc n: …", DailyMission "Tiếp tục: …", Player "Bỏ qua ▸ / Tiếp tục ▸"."

---

## 1. Khung danh sách — số đo theo frame

| | Phone 390×844 | iPad dọc 834×1194 | iPad ngang 1194×834 |
|---|---|---|---|
| Shell | padding `55px 16px 34px` (không footer) / `…16px 44px` (có footer), gap dọc 12 (Player & Stairs 10) | padding 24, gap 16 | padding `20px 24px 24px`, cột nội dung `max-width:1080` căn giữa, gap 16 (A6: 20) |
| Header | cao **56**, gap 10: Back 56×56 tròn trắng `0 4px 0 #EFE2CC` glyph 22px `#B0A18E` · khối giữa `flex:1 min-width:0` (H1 **22px** Baloo 800 lh 1.1 + phụ **13px** 700 `#8A7A6D`) · ô phải 56×56 radius 18 `#FFF1C9` `0 4px 0 #EFDDA8` (🌞 18px + "3/11" 13px `#9A6B00`) | cao **64**, gap 14: Back 64×64 `0 5px 0 #EFE2CC` glyph 24 · H1 **28px** + phụ **15px** · ô phải pill cao **48** padding `0 16` radius 16 `#FFF1C9` `0 4px 0 #EFDDA8` 16px "🌞 Nhiệm vụ 3/11" | như iPad dọc |
| Vùng cuộn | `flex:1; min-height:0; overflow` + gap 10 (ô) / 8 (hàng); **fade đáy 50px** `linear-gradient(180deg,rgba(255,247,234,0),#FFF7EA)` | gap 12, fade 50 | gap 12, fade 50 |
| H2 nhóm (dính) | 15px 800 `#8A7A6D`, padding `4px 2px`, nền `#FFF7EA` (= nền dính), đuôi "· 8 từ" 12px `#B0A18E`; C8: padding `6px 2px 2px`, không đuôi | 17px + đuôi 13px | 17px + đuôi 13px |
| Lưới ô nhỏ | `repeat(3,1fr)` gap **8**; ô 114×**110** radius 18, trắng `0 5px 0 #EFE2CC`, gap 5, padding `8px 6px`, căn giữa; emoji **40**; từ **15px** Baloo 800 lh 1.1; chip 11px radius 9 padding `2px 8px`; sao 13px `letter-spacing:2px` (`#FFB020` / `#E2D5C0`) | `repeat(5,1fr)` gap **12**; ô cao **136**, emoji **56**, từ **19px** | `repeat(6,1fr)` gap 12; ô cao 136, emoji 56, từ 19px |
| Ô nhỏ · khoá | nền `#F3EADA`, `0 5px 0 #E2D5C0`, opacity .85, emoji → 🔒, tên `#A79781`, chip "Chưa mở khoá" `#EFE2CC`/`#A79781` | như phone ×1.25 | như phone ×1.25 |
| Lưới ô lớn | 2 cột, ô **171×128** radius 18 `0 5px 0 #EFE2CC` padding `10px 8px` gap 5: A12 "🚢 ship · 🐑 sheep" 17px + chip `ɪ / iː` 11px `#E2F6F1`/`#1FA396`; A13 câu 17px lh 1.2 + nghĩa 12px `#A79781` (2 dòng ellipsis); A14 emoji 28 + chip mood 11px `#FFE9DF`/`#E05A3A` + câu đầu 15px | 3 cột, cao **160**, chữ **20px** | 4 cột, cao 160, chữ 20px |
| Hàng | thẻ mở đầu ghi **64/72**; C8 dùng đúng 64 (radius 16, `0 4px 0 #EFE2CC`, padding `0 14px`, gap 10, câu 16px Baloo `nowrap ellipsis`, sao 13px). **C1 dùng 96** (radius 20, `0 6px 0 #EFE2CC`, padding `0 16px`, gap 14; đĩa 64×64 radius 18 nền theo truyện emoji 38; tên 19px; phụ 13px "{vi} · {n} cảnh"; sao 13px; chevron 22px `#B0A18E`) | C8: **2 cột hàng** (16 hàng/cột ≈ 1200px) có H2 dính; C1: ô nhỏ 3 cột căn giữa | như iPad dọc |
| Lấp chỗ trống | danh sách ngắn (C1 3 truyện): Foxy **96×93** `bob 3s` + dòng 14px `#8A7A6D`, `flex:1` căn giữa — "không kéo ô cho to" | Foxy 72 + bong bóng (B2) | như iPad dọc |
| Hàng lẻ | "căn trái, không kéo rộng, không căn giữa" | như phone | như phone |
| Empty / not-found | Vòng 1 (`EmptyState` / `NotFound`); **empty chỉ có ở C6 review**; not-found dùng chung cho 10 màn | — | — |
| Footer CTA | cao **56** radius 18: primary `#FF7A59` + `0 5px 0 #E05A3A` chữ 18px trắng · outline trắng border 3px `#C4E8E1` + `0 5px 0 #C4E8E1` chữ `#1FA396` · ghost border `3px dashed #E2D5C0` chữ 17px `#8A7A6D` | design không ghi *(đề xuất: 64, radius 20, chữ 20px — theo A9/A7)* | 64 (A9: 420×64 radius 20 20px; A6: 480×72 radius 24 26px) |

**Bảng màu & bóng vòng 3** (mọi hex trên trang, và token tương ứng đã có trong `client/tailwind.config.ts` — không hex nào là mới):

| Vai trò | Hex trên artboard | Token |
|---|---|---|
| Nền trang / nền H2 dính | `#FFF7EA` | `cream-50` |
| Ô mở / hàng / thẻ | `#FFFFFF` + bóng `0 4px 0` (hàng 64) · `0 5px 0 #EFE2CC` (ô) · `0 6px 0 #EFE2CC` (hàng 96, thẻ 22) | `shadow-card-xs/sm/card` |
| Ô khoá / mục vô hiệu | nền `#F3EADA` · bóng `0 5px 0 #E2D5C0` · chữ `#A79781` | `sand` / `sand-edge` / `sand-text` |
| Mục "xong" (nhóm nhiệm vụ, hàng truyện trống) | nền `#F6EFE2` opacity .8 | *(chưa có token — dùng `bg-cream-100` nếu khớp, nếu không thì hex)* |
| Chip mở khoá / ô Ôn tập / ô phải header | `#FFF1C9` + `0 4px 0 #EFDDA8`, chữ `#9A6B00` | `sun-50` / `shadow-chunky-sun` / `sun-700` |
| Chip today / dải đảo / CTA phụ | `#E2F6F1` · `#C4E8E1` · `#2EC4B6` · `#1FA396` · `#2EA79B` | `teal-50` / `teal-line` / `teal-500` / `teal-600` |
| CTA chính | `#FF7A59` + `0 5px 0 #E05A3A` | `coral-500` / `shadow-chunky-coral` |
| Chip mood / chip "hiện tại" | `#FFE9DF` / `#E05A3A` | `coral-50` / `coral-text` |
| Sao đầy / rỗng | `#FFB020` / `#E2D5C0` | `star` / `star-empty` |
| Chữ phụ / chữ mờ | `#8A7A6D` / `#B0A18E` / `#A79781` | `ink-500` / `ink-300` / `sand-text` |
| Banner cảnh báo / lỗi / xanh | `#FFF1C9`+`#FFDF9E` · `#FFE3E6`+`#F8A3AE`+`#C2354B` · `#E3F6E8`+`#2E8B4A` | `Notice kind='warn'/'error'/'success'` (`components/ui/Notice.tsx:10-17`) |
| Đường mòn SVG | `#EAD9BE`, `dasharray 2 18` (phone) / `2 22` (iPad) | — (đã dùng ở `Home.tsx:442`, `LevelStairs.tsx:139`) |

**Tải nặng nhất (design tự tính):** C6 phone "64 ô = 8 nhóm × 3 hàng ≈ 8×(28+3×118) ≈ **3050px** cuộn — có H2 dính nên tìm được. Phone 375: ô 109 vẫn ≥ emoji 40 + 2 dòng." · C6 iPad ngang "64 ô = 8 nhóm × 2 hàng (cuộn ≈ **1400**)" · C8 phone "32 hàng 64 + 8 H2 dính ≈ **2400** (cũ 3300)".

---

## 2. Mười ba màn trong tầm — số đo, dữ liệu xấu nhất, trạng thái

Ánh xạ loại ô (thẻ ghi chú của design): "A10 Word Pop 12 · A11 Tập âm 9 (ô IPA) · C5 Chủ đề 8 (+ô Ôn tập) · C6 WordList 8/64 · B2 SoundWordList 3 → **ô nhỏ 3/5/6**. A12 Cặp 8 · A13 Câu 10 · A14 Đoạn 8 → **ô lớn 2/3/4**. A14 chỉ hiện câu đầu 2 dòng ellipsis. C1 Truyện 3 → **hàng 96** phone, ô nhỏ 3 cột iPad căn giữa. C8 Câu 32 → **hàng 64** phone; iPad 2 cột hàng."

### C6 WordList — `/words/:topic` · `/words/review` (3 artboard, màn tham chiếu)
- **Header:** Back (`/words` khi review · `/topic/:id` khi chủ đề — giữ như code) · H1 "📚 Ôn tập hôm nay" 22/28 + phụ "**64 từ · chạm để ôn**" 13/15 · ô phải LessonChip (56 / pill 48).
- **Ô nhỏ:** emoji 40/56 · từ 15/19 · chip 🔓 `#FFF1C9`/`#9A6B00` hoặc 🔒 `#EFE2CC`/`#A79781` 11px. **Không có sao** ở màn này (design vẽ chip khoá; catalog ô "từ" có sao — đó là A10).
- **Dữ liệu xấu nhất:** **64 ô ôn tập** → 8 nhóm × (H2 28 + 3 hàng ×118) ≈ 3050px cuộn phone / ≈1400 iPad ngang. Chủ đề thường: 8 ô = 3 hàng phone, 2 hàng iPad.
- **Trạng thái:** review · chủ đề · **rỗng** (chỉ ở review, `EmptyState` Vòng 1) · not-found (id lạ) · mỗi ô khoá/mở. Design không ghi trạng thái loading.

### C1 StoryList — `/stories` (artboard phone)
- **Header:** Back "Về nhà" · "🎧 Nghe kể chuyện" + phụ "**3 truyện · nghe rồi làm quiz**" · **ô phải trống** trên artboard (màn này không bao giờ là bước nhiệm vụ).
- **Hàng 96** (xem §1) ×3, rồi Foxy 96×93 + "Foxy lấp chỗ trống khi danh sách ngắn — không kéo ô cho to." Nền đĩa theo truyện: 🦊 `#FFE7D2` · 🦁 `#FFF1C9` · 🥞 `#E2F6F1`.
- **Dữ liệu xấu nhất:** 3 truyện trên phone 390 — bài toán ngược với C6: **thừa chỗ**, không thiếu. iPad: ô nhỏ 3 cột **căn giữa**, không kéo rộng.
- **Trạng thái:** chỉ sao 0–3/hàng (truyện không bao giờ khoá, không rỗng được). Không loading, không empty.

### C8 SentenceList — `/sentences` và `?topic=` (artboard phone)
- **Header:** Back ("Quay lại" → `/topic/:id` khi lọc · "Về nhà" khi không) · "🧱 Ghép câu" + phụ "**32 câu · 8 chủ đề**" · ô phải trống.
- **Nhóm:** H2 dính 15px (emoji + tên, **không** đuôi đếm) rồi **hàng 64**. Câu VI 16px 1 dòng `ellipsis` (design cắt "Chị của con có một con búp bê em bé." thành 1 dòng), sao 13px bên phải.
- **Dữ liệu xấu nhất:** 32 hàng + 8 H2 ≈ **2400px** (cũ 3300). iPad: **2 cột hàng**, 16 hàng/cột ≈ 1200px, H2 vẫn dính.
- **Trạng thái:** lọc hợp lệ (4 hàng, **không H2**) · không lọc (16–32 hàng + ≤8 H2) · `?topic=` lạ = không lọc · sao 0–3. Không empty (không thể rỗng). Phụ đề header khi lọc: design không ghi *(đề xuất: "4 câu · {tên chủ đề}")*.

### C5 WordTopics — `/words` (chỉ có trong catalog ô)
- **Header:** Back "Về nhà" · "Từ mới hôm nay 🧩" + phụ (design không ghi) *(đề xuất: "{n} chủ đề đã mở · chạm để học")* · ô phải LessonChip.
- **Ô nhỏ, 2 loại:** ô đầu "Ôn tập" nền **`#FFF1C9`** `0 5px 0 #EFDDA8`, 📚 40, chữ "Ôn tập" 15px, chip **coral đặc** `#FF7A59`/trắng "**12 từ hôm nay**" · ô chủ đề emoji 40 · tên 15px · chip "**3/8 mở**" 11px sun (thay dòng chữ thường).
- **Dữ liệu xấu nhất:** 8 chủ đề + 1 ô ôn tập = 9 ô → 3 hàng phone, 2 hàng iPad dọc, hàng lẻ căn trái. Ôn tập 0 từ: design không ghi *(đề xuất: chip "Chưa có từ ôn" tone neutral, ô vẫn bấm được)*.
- **Trạng thái:** 4–8 chủ đề (khoá = **vắng mặt**, không có ô khoá) · chip ôn tập 0–64.

### A10–A14 — năm màn danh sách bậc (chỉ có trong catalog ô)

| Màn | Route | Ô | Nội dung ô (phone) | Dữ liệu xấu nhất |
|---|---|---|---|---|
| A10 LevelSelect (Word Pop) | `/level/word-pop` | nhỏ 3/5/6 | emoji 40 · từ 15 · sao 13 | 12 ô = 4 hàng |
| A11 SoundLevel (Tập âm) | `/level/sound-zoo` | nhỏ 3/5/6 | **IPA "/θ/" 36px Baloo `#C08457`** (không emoji) · từ ví dụ 14px `#8A7A6D` · sao | 9 ô = 3 hàng phone; **hàng lẻ** ở 5/6 cột iPad → căn trái |
| A12 PairLevel | `/level/minimal-pairs` | **lớn** 2/3/4 | "🚢 ship · 🐑 sheep" 17px 1 dòng · chip `ɪ / iː` 11px teal · sao | 8 ô = 4 hàng phone |
| A13 StarLevel | `/level/sentence-stars` | **lớn** 2/3/4 | câu EN 17px lh 1.2 (**2 dòng ellipsis**) · nghĩa VI 12px `#A79781` · sao | 10 ô = 5 hàng phone; câu dài nhất phải cắt ở dòng 2 |
| A14 VoiceLevel | `/level/story-voice` | **lớn** 2/3/4 | emoji mood 28 · chip mood 11px coral · **câu đầu** 15px 2 dòng ellipsis · sao | 8 ô = 4 hàng |

- **Header cả 5:** Back "Các bậc" → `/levels` (A10 hiện dùng "Về trang chủ/Về bản đồ" → `/`) · H1 tên bậc 22/28 + phụ = dòng đang nằm giữa thân (R3) · ô phải LessonChip.
- **Trạng thái cả 5:** 1 trạng thái (lưới) + sao 0–3 mỗi ô; A10 id lạ → `NotFound` Vòng 1. Không empty (số ô cố định). Ô khoá (mẫu catalog: 🔒 + `#F3EADA` + chip "Chưa mở khoá") **chưa tới được** với dữ liệu hiện tại — dựng sẵn trong `Tile`, không dựng nhánh màn.
- Pill "🗣️ Xem các bậc" của A10 (hiện là hàng đầu thân, `LevelSelect.tsx:29`): design không ghi *(đề xuất: bỏ — Back đã trỏ `/levels` sau khi đổi, giảm 1 hàng)*.

### B2 SoundWordList — `/sound/:ph`
- Design chỉ ghi một câu: "B2 iPad: 3 ô căn giữa, không kéo rộng, Foxy + tầng âm phía trên lấp cột" — trùng với những gì Phase 13 đã làm (`SoundTier` + `md:grid-cols-[repeat(3,200px)]` + `SpeakPrompt`).
- Còn lệch: ô phone `min-h-[120px]` (`SoundWordList.tsx:76`) vs chuẩn ô nhỏ **110**; từ 17px vs 15px; header giữa là `Chip` "Âm 2/9" thay vì H1 + phụ (đúng theo Phase 13, **không đổi** — đây là màn có tầng âm, không phải danh sách thuần).
- **Trạng thái:** thường · vào từ nhiệm vụ cũ (Back = "Nhiệm vụ") · "Chưa có audio âm này" · tip có/không · sao 0–3. Không mic, không empty.

### A3 Home — `/` (artboard iPad dọc + phone 3 banner)
- **iPad dọc 834×1194**: padding 24, gap 14. Blob 300 `#E2F6F1` góc phải trên (`right:-80; top:-90`). Header 1 hàng: Foxy 72×70 bob + "Chào con! 👋" **26px** `#F2603D` + "Giỏi lắm, tiếp tục nhé!" 15px · bên phải: pill tuần (7 chấm **24**, nền `#FFC533` / `#F3EADA` viền `2px dashed #D9CBB4`, hôm nay `0 0 0 3px #FFE9A8`; "🔥 4 ngày" 15px `#2EA79B`, radius 16 padding `10px 14px` `0 4px 0 #EFE2CC`) + pill "⭐ 128" `#FFF1C9` radius 16 padding `10px 14px` 17px `#9A6B00` + nút phụ huynh 56×56 radius 18 trắng `0 5px 0 #EFE2CC` emoji 24.
- Banner hết giờ: `#FFF1C9` border `3px #FFDF9E` text `#9A6B00` radius 16 padding `10px 14px`, tiêu đề 14px + phụ 12px opacity .85.
- MissionCard hàng ngang: trắng radius 22 padding `16px 18px` `0 6px 0 #EFE2CC`; "🌞 Nhiệm vụ hôm nay" 19px + "3/11" 16px `#2EA79B`; bar cao **11** radius 8 nền `#F1E7D4` fill `#2EC4B6` 27%; CTA **220×64** radius 20 coral 20px "Tiếp tục ▸".
- "🏝️ Đảo chủ đề" 16px `#8A7A6D`. Lưới đảo **3 cột `grid-auto-rows:1fr`** gap 12, ô cao **150** radius 22 `0 6px 0 #EFE2CC`: emoji 48 · tên 20px · sao 14px · phụ 12px ("Luyện thêm" `#2EA79B` / "Chưa mở khoá" `#B0A18E`); khoá: 🔒 + `#F3EADA` + `0 6px 0 #E2D5C0` + opacity .85. **Ô thứ 9 trong lưới = "🗣️ Các bậc luyện nói"** (trắng radius 22, 19px `#2EA79B`) → "8 đảo + ô Speak Lab = 9 ô, 3 hàng vừa khít".
- **Phone 3 banner**: padding `56px 16px 44px` gap 10. Header: Foxy 54×52 + "Chào con! 👋" 19px + "🔥 4 ngày · ⭐ 128" 13px (⭐ `#9A6B00`); 7 chấm **14px** gap 6 bên phải. Banner 1 (⚠️ hết giờ) padding `9px 12px` icon 18 chữ 13px, **không có ✕**. Banner 2 (ℹ️ mốc email) `#E2F6F1`/`#C4E8E1`/`#1FA396` padding `6px 6px 6px 12px` + nút "Liên kết" cao 40 padding `0 10` radius 12 `rgba(255,255,255,.7)` 12px + ✕ 40×40 16px opacity .6. Rồi dòng gộp: "**+1 thông báo (Thêm vào Màn hình chính) ▸**" 12px 800 `#8A7A6D` căn giữa.
- MissionCard phone: radius 22 padding `14px 16px`; tiêu đề 17px + "3/11" 15px; bar 11px margin `9px 0 11px`; CTA cao **56** radius 18 18px.
- "🏝️ Đảo chủ đề" 15px. Lưới đảo **2 cột gap 10, ô cao 110** ("thấp hơn 128 cũ để lưới thấy 2 hàng dù có 2 banner"): emoji 34 · tên 15px · sao 11px (không có dòng phụ). Fade đáy 40. Hàng chân: "🗣️ Các bậc luyện nói" `flex:1` cao 56 trắng radius 18 `0 5px 0 #EFE2CC` 16px `#2EA79B` + nút phụ huynh 56×56.
- Quy tắc banner (nguyên văn): "xếp theo ưu tiên ⛔ lỗi → ⚠️ hết giờ → ℹ️ mốc email → ℹ️ A2HS. **Tối đa 2** hiện; còn lại gộp thành dòng "+N thông báo ▸" (chạm mở sheet). Banner hết giờ không tắt được; 2 info có ✕ 40 (hit 44) và nút hành động 40."
- Khác: "Home iPad dọc không có link "Đã dùng Speak Up rồi?" khi đã có lịch sử; khi máy mới, link 44px nằm dưới MissionCard." · "Panel streak khi chạm 7 chấm: xem Q3 Vòng 1 (bottom sheet phone / popover iPad)" — đã có `StreakPanel` (Phase 12).
- **Trạng thái phải vẽ:** 0/1/2/3 banner (3 → 2 + dòng gộp) · MissionCard 4 trạng thái (R15) · đảo mở/khoá · Foxy 3 mood theo tiến độ hôm nay · link khôi phục có/không · iPad **ngang** = bản đồ cũ, **không đổi vòng này**. Dữ liệu xấu nhất: **iPad dọc, 2 banner + MissionCard "xong" + panel streak mở** — 9 ô lưới vẫn phải đủ 3 hàng trong 1194.

### A4 MissionCard — 4 trạng thái
Thẻ **300×128 cố định**, radius 22, padding `14px 16px`, `0 6px 0 #EFE2CC`, border `2px #F1E7D4`, "nhãn không gãy dòng". Tiêu đề 16px; bar 11px; CTA cao **48** radius 16 **17px `nowrap`**.

| Trạng thái | Đếm | Bar | CTA |
|---|---|---|---|
| rỗng | "—" `#B0A18E` | 0% | "Luyện tự do →" trắng/`#1FA396`, `0 4px 0 #C4E8E1, inset 0 0 0 3px #C4E8E1` |
| chưa làm | "0/11" `#8A7A6D` | 0% | "Bắt đầu ▸" coral |
| đang làm | "3/11" `#2EA79B` | 27% `#2EC4B6` | "Tiếp tục ▸" coral |
| xong | "✓ 11/11" `#2E8B4A` | 100% `#7ED99A` | "**Chơi lại 🎉**" teal `#2EC4B6` `0 4px 0 #1FA396` |

### A6 DailyMission — `/mission` (2 artboard)
- **Header:** Back "Về trang chủ 🏠" (iPad ngang: "Về bản đồ 🏝️", R14) · "Nhiệm vụ hôm nay 🌞" + phụ "5 bước nhỏ — 15 phút thôi!" (rỗng: "Bậc ⭐ 2 · Đọc từ") · ô phải = 2 chip trên iPad, trống trên phone (`LessonChip` tự ẩn ở `/mission`, `LessonChip.tsx:13`).
- **Trạng thái:** rỗng · chưa làm ("Bắt đầu ▸") · đang làm ("Tiếp tục: {nhóm} ▸") · xong (CTA "Về trang chủ 🏠") · 1–5 nhóm × 5 loại (🎧 🗣️ 🧩 🧱 🔁) × 3 trạng thái nhóm (xong / hiện tại / tới). **Dữ liệu xấu nhất: 5 nhóm iPad ngang** — 5 cột 200 + footer trong 834 (cũ tràn 1189, `README` Phase 12).
- **Rỗng (phone, có CTA)**: header phụ "Bậc ⭐ 2 · Đọc từ"; khối giữa `flex:1` căn giữa gap 12: Foxy 120×116 bob · "Hôm nay chưa có nhiệm vụ" **22px** · "Bé có thể luyện tự do ở bất kỳ đảo nào — hoặc leo các bậc luyện nói." 14px; 2 nút 56: "Luyện tự do →" primary + "Về trang chủ 🏠" outline teal.
- **iPad ngang 5 nhóm**: header 64 + phụ "5 bước nhỏ — 15 phút thôi!" + 2 chip trong **header phải** (`Bậc ⭐ 2 · Đọc từ` sun 15px radius 12 padding `8px 14px` · `1/5 nhóm xong` teal). Lưới `repeat(5,1fr)` gap 16 `align-content:center`, thẻ cao **240** radius 22 padding `16px 12px` gap 8: emoji 52 · tên 20px · phụ 14px · chip 14px radius 11 padding `5px 12px`. Trạng thái: xong (`#F6EFE2`, opacity .8, tên `#A79781`, chip "✓ Xong" `#E3F6E8`/`#2E8B4A`, phụ "1/1 · xong rồi!") · hiện tại (`0 6px 0 #1FA396, 0 0 0 4px #2EC4B6`, phụ "2/5 · **bắt đầu ở đây!**" `#2EA79B`, chip coral "≈ 5'") · tới (chip sun "≈ 3'", phụ "0/3 · bước 3"). Footer: Foxy cheer 80×77 + CTA **480×72** radius 24 **26px** "Tiếp tục: 5 thẻ phát âm 🗣️" — "footer CTA trong 834 (cũ tràn 1189)".

### A7 MissionComplete — `/mission/done` (biến thể 0 sao)
- **Header:** không có (màn ăn mừng, cột giữa toàn màn). **Trạng thái:** 0 sao (vẽ) · ≥1 sao (giữ bản Phase 12: confetti + Foxy cheer + pill "+n ⭐") · streak 0 (câu riêng) · streak ≥1. Dữ liệu xấu nhất: **0 sao + streak 0** — hai nhánh cùng lúc, và vẫn phải vừa 667px không cuộn.
Nền gradient `#FFF7EA → #FFEFD9`; cột giữa padding `0 26px 30px` gap 14: Foxy **happy** 150×144 `bob 2.4s` · H1 **30px** 2 dòng "Xong nhiệm vụ rồi! 🦊 / Con đã rất cố gắng." · thẻ trắng radius 18 padding `12px 26px` `0 5px 0 #EFE2CC` 18px `#8A7A6D` "Mai làm lại để lấy ⭐ nhé" (thay pill "+0 ⭐") · 7 chấm **34px** gap 7 (hôm nay ⭐ + `0 0 0 4px #FFE9A8`) · "🔥 Bắt đầu chuỗi mới từ hôm nay!" 15px `#2EA79B` · CTA cao **64** padding `0 40` radius 20 teal `0 5px 0 #1FA396` 20px "Về trang chủ 🏠". Ghi chú: "0 sao: Foxy **happy** (không cheer), **không confetti**".

### A8 TopicHub — `/topic/:id` (phone, đảo không có truyện)
- **Trạng thái:** đảo có truyện (3/8 đảo, 3 hàng có sao → "⭐ n/9") · **đảo không có truyện (5/8 đảo, vẽ)** → "⭐ n/6" + thẻ mờ · hàng trong nhiệm vụ hôm nay (viền teal + chip) · đảo khoá / id lạ → `LockedTopic` · sao 0–3 mỗi hàng. Dữ liệu xấu nhất: **2 truyện + cả 3 hàng đều "có trong nhiệm vụ"** — 4 hàng 84 + khối tên 236 + CTA 56 trong 844.
- Dải teal `#2EC4B6` cao **236** radius `44 44 40 40`, "dải teal 236 và header là **một khối** (dải = nền của header + khối tên)". Header trên dải: Back 56 `rgba(255,255,255,.92)` glyph `#1FA396`; **giữa = chip "⭐ 2/6 sao đảo"** (`rgba(255,255,255,.92)` radius 12 padding `7px 14px` 15px `#1FA396`); phải = LessonChip 56 sun.
- Khối tên: đĩa 84×84 trắng `0 6px 0 #1FA396` emoji 42 · tên **28px trắng** · dòng 13px `#D3F1EC` "Đảo số 5 · ★★☆ · Luyện thêm nhé!" (sao `#FFE59E`).
- 3 hàng `min-height:84` radius 22 padding `14px 16px` `0 6px 0 #EFE2CC` gap 14: emoji 32 · "Từ mới **3/8**" 18px (đếm teal trong tiêu đề) · chip today 11px teal radius 9 padding `3px 9px` · chevron 22px. Hàng trong nhiệm vụ: border `3px #2EC4B6`. Hàng "Ghép câu 1/4" hiện **sao 13px** thay dòng đếm phụ.
- Hàng truyện trống: `#F6EFE2` `0 6px 0 #E2D5C0` opacity .8, emoji `filter:grayscale(1)`, tiêu đề `#A79781`, dòng 12px "**Đảo này chưa có truyện — nghe truyện ở 3 đảo khác nhé**", chip "Sắp có 📖" `#F3EADA`/`#A79781`, **không bấm được**.
- Spacer `flex:1` rồi CTA ghim 56 coral "**Học tiếp: Từ mới 3/8 ▸**". Ghi chú: ""⭐ n/m sao đảo": m = 3 × số mục có sao (6 khi không truyện, 9 khi có) … CTA "Học tiếp" trỏ mục dở đầu tiên; đủ 3★ hết → "Luyện lại: …"."

### A9 LevelStairs — `/levels` (phone + iPad ngang)
- **Header:** Back "Về trang chủ 🏠"/"Về bản đồ 🏝️" · "Các bậc luyện nói 🗣️" + phụ "Leo từng bậc — mỗi bậc một trò mới!" — **design dùng một tiêu đề ở cả 3 frame**, bỏ nhánh "Speak Lab 🗣️" của `md:` (`LevelStairs.tsx:118-119`) · ô phải trống.
- **Trạng thái:** 5 bậc, tất cả mở (trạng thái khoá 🔒 + "Sắp có" vẽ sẵn nhưng dữ liệu chưa tới được) · Foxy đứng ở bậc chưa 3★ đầu tiên (hết 3★ → bậc cuối) · tag "ĐANG HỌC" / "✓" / rỗng · sao 0–3 mỗi bậc · CTA luôn có. Dữ liệu xấu nhất: **phone 375×667** — 5 ô 84 + Foxy + CTA 56; design hạ bậc xuống 72 ở đó.
- **Phone**: vùng bậc `flex:1; min-height:0` `column-reverse` `justify-content:space-between` padding `6px 0`; SVG `viewBox 0 0 350 560` `preserveAspectRatio=none` path bezier, stroke 9 `#EAD9BE` `dasharray 2 18`. Ô **236×84** radius 20 padding `0 14px` gap 10: emoji 30 · tên 16px · sao 12px · tag 12px `#B0A18E` ("ĐANG HỌC" / "✓" / rỗng). Hiện tại: `0 6px 0 #1FA396, 0 0 0 3px #2EC4B6` + Foxy **58×56** `margin-left:-6` cạnh ô. Khoá: `#F3EADA` `0 5px 0 #E2D5C0` 🔒. CTA ghim 56 "Luyện bậc 2: Đọc từ 🎈". Ghi chú: "5 ô 236×84 zigzag, gap tự chia theo chiều cao (space-between). Vùng bậc **overflow-y:auto**: trên 667 (bậc 72px) vẫn vừa; nếu không vừa thì cuộn, bậc 1 thấy trước (cuộn về đáy khi mount)."
- **iPad ngang**: bỏ 5 margin cố định — "Ô i (0–4) đặt **left = 10% + i·20%** (giữa cột), **top = 70% − i·17.5%** của body → co theo mọi chiều cao iPad (692–834). Ô **176×176**. Đường mòn SVG cùng hệ %, preserveAspectRatio none (đã chấp nhận). **iPad dọc dùng zigzag phone với ô 300×96**." Ô: radius 26 padding 12 gap 6, emoji 52 · tên 19px · sao 14px · tag 12px `min-height:14`; hiện tại `0 8px 0 #1FA396, 0 0 0 4px #2EC4B6` + Foxy 80×77 phía trên (`margin-bottom:-6`). SVG `viewBox 0 0 1080 600` stroke 10 `dasharray 2 22`. **CTA 420×64 radius 20 20px căn giữa — có ở iPad.**

### C2 StoryPlayer — `/story/:id` (phone)
- **Trạng thái:** chưa bắt đầu (không từ nào sáng) · đang phát ❚❚ / tạm dừng ▶ · hết truyện (CTA đổi) · nhiệm vụ vs tự do (CTA đổi) · **chưa có giọng đọc** (dòng info 44) · **không phát được / iOS chặn autoplay** (dòng lỗi 44 + "Thử lại" + ▶ pulse) · phụ đề bật/tắt · tốc độ 1 ↔ 0.75 · cảnh 1..N · not-found. **Dữ liệu xấu nhất: karaoke 9 từ / 40 ký tự** (cảnh dài nhất) — phải **2 dòng** trong 358px.
- Header 56: Back 56 · **giữa = cột 2 dòng** (chip "Cảnh 2/7" teal 15px radius 12 padding `7px 14px` + "🦊 The Little Fox" 11px `#B0A18E`) · phải LessonChip 56. Không còn overlay nào trên tranh.
- Tranh `aspect-ratio:16/9` radius 22 `0 6px 0 #EFE2CC`, gradient theo truyện, **emoji 120**; "chỗ ảnh 16:9 sẵn".
- Bar 11px radius 8 `#F1E7D4` fill teal 42%.
- Khối karaoke `flex:1` căn giữa gap 8: dòng "👆 Chạm 1 từ để nghe lại" 13px `#2EA79B` · từ đang đọc **28px** `#F2603D`, đã đọc **21px** `#CDBFA9`, chưa **21px** `#4A3B33`, mỗi từ padding `8px 6px` `min-height:44` `display:inline-flex`, wrap gap `2px 4px` · phụ đề VI 14px `#A79781`.
- Điều khiển: ⏮ **64 tròn** trắng `0 5px 0 #EFE2CC` 22px · ▶ **96 tròn** teal `0 7px 0 #1FA396` 38px · ⏭ 64, gap 18. Hàng tuỳ chọn: pill tốc độ trắng radius 14 padding 5 chứa 2 chip **44×40** radius 11 (đang chọn `#FFE9DF` + `inset 0 0 0 3px #FF9A62`) · cụm phụ đề `min-height:44`: "🇻🇳 Phụ đề" 14px + toggle **50×28** radius 14 teal, knob 22.
- CTA ghost 56 radius 18 `3px dashed #E2D5C0` 17px "Bỏ qua ▸" → hết truyện: primary pulse "**Tiếp tục: Quiz 3 câu →**"; tự do: ghost "Về danh sách truyện".
- 2 trạng thái audio (dòng 44px thay dòng chữ trần, "nằm giữa thanh tiến trình và karaoke, thay chỗ dòng "👆 Chạm 1 từ""): info `#E2F6F1`/`3px #C4E8E1`/`#1FA396` radius 14 padding `6px 12px` "ℹ️ Chưa có giọng đọc — chữ chạy theo nhịp ước lượng" · lỗi `#FFE3E6`/`3px #F8A3AE`/`#C2354B` + nút "**Thử lại**" cao 36 padding `0 12` radius 10 trắng 13px, "🔇 Không phát được giọng đọc"; "iOS chặn autoplay → dòng đỏ + nút ▶ pulse".
- Phụ đề "mặc định **bật** (≥700 cao) / tắt (<700)". iPad: "cùng footer trong cột "làm" (tranh + karaoke bên trái **616**, điều khiển + CTA bên phải **440**)".

### C3 StoryQuiz — `/story/:id/quiz` (hỏi + kết quả 0/3)
- **Trạng thái:** hỏi idle · **sai** (ring hồng + 🙈 + Foxy surprised + banner vàng, không tự chuyển, không lộ đáp án) · **đúng** (ring xanh + ✅ + banner xanh, tự chuyển 900ms, khoá chạm) · câu 1..3 · kết quả **0/3, 1/3, 2/3, 3/3** (4 nhánh, không phải 3) · nhiệm vụ vs tự do (nút thứ 3 đổi). **Dữ liệu xấu nhất: kết quả 0/3** — 2 nút 56 + 1 link 44 + Foxy 130 + sao 44 trong 844 (và 667 ở `short:`).
- Hỏi: header 56 (Back · chip "Câu 1/3" teal · LessonChip). Hàng hỏi: Foxy 54×52 + bong bóng trắng radius 16 (góc trái dưới 6) padding `10px 12px` `0 4px 0 #EFE2CC`: câu EN 17px + VI 12px + nút 🔊 **44×44** tròn teal `0 4px 0 #1FA396` 17px.
- 3 thẻ đáp án `flex:1 min-height:96` radius 22 trắng, **hàng ngang** gap 18: emoji **56** + nhãn **20px**; sai `0 6px 0 #F8A3AE, 0 0 0 5px #FFD4DA` + 🙈 `absolute right:12 top:10` 24px; đúng ring xanh + ✅, tự chuyển 900ms. Banner cao **48** radius 14 `#FFF1C9`/`#9A6B00` 15px.
- Kết quả 0/3: cột giữa padding `0 24px 30px` gap 14: Foxy **idle** 130×126 · sao **44px** `letter-spacing:4px` **cả 3 rỗng `#E2D5C0`** · "Bé trả lời đúng 0/3" 24px · "Không sao! Nghe lại truyện một lần rồi thử lại nhé." 14px · primary 56 "🎧 Nghe lại truyện" · outline teal 56 "Làm quiz lại" · **link chữ `min-height:44` 15px gạch chân** "Về nhiệm vụ →" ("không phải nút 64"). Thang: "3 đúng 3★ cheer · 2 đúng 2★ happy · 1 đúng 1★ idle · **0 đúng 0★ idle**, primary đổi thành "Nghe lại truyện" (3/3 → primary "Kể lại câu chuyện →")". iPad: "3 thẻ đáp án 1 hàng, mỗi thẻ `flex:1` max **300**, tỉ lệ **4:3**, emoji **96**".

---

## 3. Trả lời Q11 · Q12 · Q13 · Q14 (nguyên văn trên artboard) · Q19

- **Q11 — Karaoke 64px/từ?** "**Trả lời: không.** Từ karaoke là **mục tiêu phụ** (nghe lại 1 từ), không phải hành động chính → hit **44×44** (padding 8px 6px quanh chữ 21px), gap 4. 9 từ × ≈ 52px vừa 2 dòng trong 358. Floor 64 chỉ áp cho hành động chính (play, mic, CTA, ô đáp án)."
- **Q12 — Toggle nhạc nền.** "**Trả lời: bỏ hẳn** — code không có nhạc nền, không vẽ tính năng không tồn tại. Hàng tuỳ chọn chỉ còn **pill tốc độ 🐢/🐇** (chip 44×40) bên trái và **toggle phụ đề thật** 50×28 có nhãn "🇻🇳 Phụ đề" bên phải (hit 44 cả cụm)."
- **Q13 — Nút Tiếp tục / Bỏ qua ở player.** "**Trả lời: giữ, là footer chuẩn của khung.** Đang phát: ghost 56 "Bỏ qua ▸" (đứt nét, không nổi). Hết truyện: primary pulse "Tiếp tục: Quiz 3 câu →". Tự do (không nhiệm vụ): ghost "Về danh sách truyện". Không có nút này trẻ không sang được quiz."
- **Q14 — Quiz: ảnh hay emoji?** "**Trả lời: emoji**, vì ràng buộc kỹ thuật "không có ảnh cảnh truyện". Thẻ đáp án phone = hàng flex:1 (min 96) emoji 56 + nhãn 20px; iPad = 3 thẻ 4:3 emoji 96. Thẻ nhận **ảnh 16:9 thay emoji** khi dữ liệu có `image` — không đổi bố cục. Sai: ring hồng + 🙈; đúng: ring xanh + ✅, tự chuyển 900ms."
- **Q19 (19 màn chưa có design)** — **không trả lời bằng tên**. Trang không nhắc Q19 ở đâu; nhưng nó phủ 10 màn danh sách bằng một khung chung + catalog ô, nên phần "màn danh sách" của Q19 coi như đã đóng. Vẫn còn hở: **A10–A14, C5, B2, C1 iPad, C8 iPad, A8 iPad, A7 iPad, A6 iPad dọc, C2/C3 iPad** không có artboard riêng (chỉ ghi chú), và **`short:` 375×667** chỉ có 2 câu (C6 ô 109; A9 bậc 72; phụ đề tắt <700).

---

## 4. Mâu thuẫn với code (Phase 12/13) & câu hỏi mở

| # | Vấn đề | Code hôm nay | Design nói | Đề xuất |
|---|---|---|---|---|
| R1 | **Chưa có component danh sách** | 7 call-site tự viết lưới + `CARD_LINK` `flex flex-col items-center gap-2 rounded-xl3 bg-white p-6 shadow-card` (`client/src/components/ui/cardLink.ts:3`), dùng ở `WordList.tsx:49`, `WordTopics.tsx:28,36`, `StoryList.tsx:16`, `PairLevel.tsx:26`, `StarLevel.tsx:24`, `VoiceLevel.tsx:24` | 2 cỡ ô + 2 cỡ hàng + H2 dính, cùng số cột ở mọi màn | Thêm `ListGrid` (`size='sm'\|'lg'` → 3/5/6 hoặc 2/3/4 cột, gap 8/12), `Tile` (emoji\|ipa\|mood + title + sub? + chip? + stars?), `ListRow` (`h=64\|96`), `StickyGroup`. Xoá `CARD_LINK` khi 7 call-site đã chuyển |
| R2 | **Header căn giữa vs căn trái, không có ô phụ đề** | `PageHeader` là grid `[56px_1fr_56px]`, cụm giữa `justify-self-center` `flex-col items-center` (`components/ui/page/PageHeader.tsx:11,13`); H1 mọi màn danh sách là `text-[22px] … md:text-[32px]` (vd `WordList.tsx:31`, `StoryList.tsx:11`) | H1 22/**28** + phụ 13/15 **căn trái**, ô giữa `flex:1 min-width:0` | `PageHeader` nhận `title`/`sub` (hoặc `align='start'`): cột giữa thành `1fr` stretch, `justify-self-stretch text-left`, H1 `md:text-[28px]`. Giữ nguyên đường đi của `dimmed`/`engine`/`right` (Phase 12/13) |
| R3 | **Dòng phụ đang nằm giữa thân** | `<p className="text-center text-[15px] … md:text-lg">` ở `LevelSelect.tsx:31`, `SoundLevel.tsx:18`, `PairLevel.tsx:18`, `StarLevel.tsx:16`, `VoiceLevel.tsx:19`, `WordTopics.tsx:25`, `LevelStairs.tsx:126`, `DailyMission.tsx:127` | Dòng phụ là **phần của header**; thân bắt đầu bằng lưới | Chuyển 8 chuỗi này thành `sub` của header; C6 lấy phụ đề mới "64 từ · chạm để ôn", C8 "32 câu · 8 chủ đề", C1 "3 truyện · nghe rồi làm quiz" |
| R4 | **Số cột sai ở cả 7 lưới, và `lg:` không phân biệt 2 hướng iPad** | `WordList.tsx:45` `grid-cols-2 md:grid-cols-4`; `WordTopics.tsx:27` `2/md:3`; `StoryList.tsx:14` `2/md:3`; `LevelSelect.tsx:33` `2/md:3/lg:4`; `SoundLevel.tsx:20` `2/md:3` + `min-h-[168px]` (`:26`); `PairLevel.tsx:20` `1/md:2/lg:4`; `StarLevel.tsx:18` & `VoiceLevel.tsx:21` `1/md:2/lg:3` | ô nhỏ 3/5/6, ô lớn 2/3/4 | `ListGrid` phát `grid-cols-3 md:grid-cols-5 ipad:grid-cols-6` / `grid-cols-2 md:grid-cols-3 ipad:grid-cols-4`. **Bỏ mọi `lg:`** (1024 bắt cả iPad dọc xoay ngang lẫn desktop) — luật 3 frame của Phase 12 |
| R5 | **Emoji/chữ trong ô to gấp rưỡi** | `text-[64px]` ở `WordList.tsx:50`, `WordTopics.tsx:29,37`; `text-[72px]` `StoryList.tsx:17`; `text-[56px]` `LevelSelect.tsx:40`, `VoiceLevel.tsx:29`, IPA `SoundLevel.tsx:28`; từ `text-[24px]`/`text-[26px]` | emoji 40/56, từ 15/19, IPA **36** | Áp số của catalog. Đây là nguyên nhân trực tiếp của `phone/words-animals-full.png` (8 ô 2 cột ≈ 1900px cuộn trong 738px) |
| R6 | **Không màn nào nhóm theo chủ đề trừ C8, và C8 không dính** | `SentenceList.tsx:36,42` nhóm `gap-7` + `h2 text-[26px] mb-3` **không sticky**; `WordList.tsx:15–17` review là danh sách **phẳng** từ `dueWords()` | H2 dính 15/17px + đuôi đếm; **C6 review nhóm theo chủ đề** (8 nhóm) | `StickyGroup` (`sticky top-0 z-10 bg-cream-50`); `WordList` review nhóm `dueWords()` theo `findTopic` trước khi vẽ (thứ tự `TOPICS`) |
| R7 | **Năm màn bậc là 5 bản sao shell** | `LevelSelect.tsx`, `SoundLevel.tsx`, `PairLevel.tsx`, `StarLevel.tsx`, `VoiceLevel.tsx` — cùng `PageShell/PageHeader/PageBody` + 1 lưới | Design chỉ phân biệt chúng bằng **loại ô** (catalog) | Giữ 5 route và 5 file mỏng (back/tiêu đề/nguồn dữ liệu khác nhau: `/levels` vs `/`), nhưng thân = `ListGrid` + `Tile` chung. **Không** gộp thành một màn cấu hình bằng bảng: `LevelSelect.tsx:16` còn nhánh chuyển hướng `sound-zoo → SoundLevel`, và 5 reducer sao (`LevelStairs.tsx:49–92`) đã cho thấy "một bảng cho tất cả" đắt hơn 5 file mỏng |
| R8 | **C1 lưới → hàng** | `StoryList.tsx:14` lưới 2 cột phone, ô `CARD_LINK p-6` emoji 72 | phone: **3 hàng 96** + Foxy lấp chỗ trống; iPad: ô nhỏ 3 cột căn giữa | `ListRow h=96` + `SpeakPrompt`-kiểu Foxy 96×93 (không bong bóng, chỉ dòng chữ) — **thẻ mở đầu của design ghi hàng "64/72" nhưng C1 vẽ 96**: lấy 96 (artboard + ghi chú đều nói 96), 64 chỉ cho C8 |
| R9 | **C8 hàng quá cao, chữ quá to, sao quá to** | `SentenceList.tsx:9-10` `ROW` `min-h-[80px] … px-6 py-3 rounded-xl3`; câu `text-[24px]` không truncate (`:53`); `StarRow` mặc định `md` = 28px (`components/ui/Stars.tsx:2`) | hàng **64** radius 16 padding `0 14`, câu **16px** 1 dòng ellipsis, sao **13px**; iPad **2 cột hàng** | `ListRow h=64`, `truncate`; `Stars size="xs"` là 12px — thêm mốc **13px** hoặc dùng `xs` *(đề xuất: dùng `xs`, lệch 1px)*; iPad: `md:columns-2` hoặc `md:grid-cols-2` trên danh sách hàng, H2 vẫn dính |
| R10 | **C5 ô Ôn tập & chip đếm** | `WordTopics.tsx:31` `Chip tone="sun" className="text-[22px]"` "Ôn tập hôm nay (N)"; `:39` dòng `text-lg` "N/8 đã mở khoá" | ô Ôn tập nền `#FFF1C9` + chip **coral đặc** "12 từ hôm nay" 11px; chủ đề: chip "3/8 mở" 11px | `Tile` nhận `variant='accent'` (nền sun) và chip `tone='coralSolid'` — `Chip` hiện chỉ có 4 tone nền nhạt (`components/ui/Chip.tsx:6-11`) → thêm 1 tone hoặc dùng `className` |
| R11 | **`PageBody` không có fade đáy khi màn không có footer** | fade 40px là `before:` của `PageFooter` (`components/ui/page/PageFooter.tsx:7`); C6/C8/C5/A10–A14 không có footer → không fade | vùng cuộn có fade **50px** ở mọi màn danh sách | `PageBody` nhận `fade?` (pseudo-element cùng công thức, 50px) — bật cho màn danh sách không footer |
| R12 | **Banner Home: dòng gộp không bấm được và không nêu tên** | `NoticeStack.tsx:17` `<div className="text-center text-[12px] …">+{rest} thông báo</div>`; `max = 2` mặc định (`:9`); thứ tự ưu tiên `error 0, warn 1, … info 5` (`:7`) — **khớp design** | "+1 thông báo (**Thêm vào Màn hình chính**) ▸", "chạm mở sheet" | Dòng thành `<button min-h-[44px]>` nêu **tiêu đề banner bị ẩn đầu tiên** + "▸", mở `Dialog` (Phase 12) liệt kê phần còn lại. Ưu tiên giữ nguyên |
| R13 | **Home iPad dọc nhận lưới 2 cột của phone** | `Home.tsx:410` `grid grid-cols-2 … ipad:block`; đảo `h-32 md:h-40` (`:140-141`); Speak Lab + link phụ huynh là **hàng chân** `col-span-2` (`:515-525`); streak + ⭐ là hàng riêng trong thân (`:383-403`) | iPad dọc: lưới **3 cột** `auto-rows:1fr` ô **150**, **ô thứ 9 = Speak Lab trong lưới**, streak + ⭐ + nút phụ huynh **trong header** | `md:grid-cols-3 md:auto-rows-fr`; ô Speak Lab thành phần tử lưới ở `md:` (vẫn `ipad:absolute` ở bản đồ); chuyển cụm streak/⭐ vào `PageHeader right` từ `md:` lên. Đảo phone `h-32` → **110** ("thấp hơn 128 cũ để lưới thấy 2 hàng dù có 2 banner") |
| R14 | **Copy "Về bản đồ" ở iPad dọc** | `HomeLabel.tsx:16-17` và `BackButton.mdLabel` chia ở **`md:` (768)**, dùng ở `DailyMission.tsx:112,193`, `MissionComplete.tsx:58`, `LevelSelect.tsx:22`, `LevelStairs.tsx:113`, `StoryQuiz.tsx:110` | "Copy "Về bản đồ" **chỉ khi Home là bản đồ (iPad ngang)**. Phone & iPad dọc: "Về trang chủ 🏠"" | Đổi mốc của `HomeLabel`/`mdLabel` từ `md:` sang **`ipad:`** (`hidden ipad:inline`). Sửa đúng A17 trong inventory — 768–1023 và iPad dọc đang hứa bản đồ mà nhận lưới |
| R15 | **MissionCard: cao thay đổi, nhãn "xong" gãy 2 dòng, thừa 1 dòng** | `components/MissionCard.tsx:19` không cố định cao; `:27-29` dòng "Hoàn thành! 🎉" riêng; `:34-36` nhãn "Hoàn thành rồi! 🎉 Chơi lại?"; CTA là `Button` md (56/64) | **300×128 cố định**, bỏ dòng "Hoàn thành! 🎉", nhãn xong = "**Chơi lại 🎉**" teal, rỗng = "Luyện tự do →" outline + đếm "—", CTA cao **48** radius 16 `nowrap` | Thêm trạng thái `empty` (total 0) vào `MissionProgress`; CTA 48 không phải size `Button` nào (`Button.tsx:22-26` md=56/64) → **thêm size `sm` 48** hoặc `className` đè. Ghi rõ trong spec để không đẻ ra `CTA_PHONE` thứ tư |
| R16 | **DailyMission rỗng không có CTA** | `DailyMission.tsx:118-124` `EmptyState` (có 1 nút outline 44) và `:200-202` footer **không render gì** khi rỗng | Foxy 120 + 22px + 14px + **2 nút 56**: "Luyện tự do →" primary + "Về trang chủ 🏠" outline | Giữ `EmptyState` cho thân (bỏ `cta`), thêm `PageFooter` 2 nút. `EmptyState` hiện cố định emoji 34/tiêu đề 16 (`EmptyState.tsx:9-10`) < design (120/22/14) → thêm `size='hero'` |
| R17 | **DailyMission iPad: chip trong thân, thẻ cao tự do, CTA nhỏ** | `DailyMission.tsx:131-134` 2 chip nằm trong thân; `GROUP_CARD` `md:h-auto` (`:72-75`); phụ = "Bước {i+1} · bắt đầu ở đây!" (`:164-167`); chip "≈ {n} phút" (`:176`); footer Foxy 96 + `Button lg flex-[1.35]` (`:187-198`) | 2 chip trong **header phải**; thẻ cao **240**; phụ "2/5 · bắt đầu ở đây!"; chip "**≈ 5'**"; CTA **480×72 26px** + Foxy 80 | Chuyển 2 chip lên `right` của header; `ipad:h-[240px]`; đổi copy phụ sang "{done}/{total} · …"; rút "phút" thành "'". CTA 72×26px = `Button lg` iPad (72/26) ✔, chỉ cần `ipad:w-[480px]` |
| R18 | **MissionComplete không có biến thể 0 sao** | `MissionComplete.tsx:31` `Confetti` **vô điều kiện**; `:33` Foxy `cheer`; `:45-47` pill "+{n} ⭐" kể cả khi 0; H1 "Nhiệm vụ hoàn thành! 🎉"; `:53-54` "🔥 Chuỗi 0 ngày liên tiếp — giỏi lắm!" khi streak 0 | 0 sao: Foxy **happy**, **không confetti**, thẻ trắng "Mai làm lại để lấy ⭐ nhé" thay pill, H1 "Xong nhiệm vụ rồi! 🦊 / Con đã rất cố gắng.", streak 0 → "🔥 Bắt đầu chuỗi mới từ hôm nay!" | Thêm nhánh `starsToday === 0` và nhánh `streak === 0`. Chấm tuần 34px = mặc định `WeekDots` ✔ |
| R19 | **TopicHub: header không nằm trong dải teal, không có chip sao đảo, không có CTA ghim** | `TopicHub.tsx:100-102` header trên nền cream với H1 = tên đảo; `:111-115` dải teal là `div` trang trí **trong thân**, `md:hidden`, cao `180 + safe-top`; `:132` `StarRow` trong thân; **không có `PageFooter`** | Dải 236 là **nền của header + khối tên**; header giữa = chip "⭐ 2/6 sao đảo"; tên 28px trắng trong thân; CTA ghim 56 "Học tiếp: …" | `PageHeader` nhận `onBand?` (nền trong suốt, `BackButton variant="onArt"`-kiểu trắng .92 + glyph teal) — đây là **ngoại lệ đầu tiên** với luật Phase 12 "header luôn trên cream", phải ghi thành Ruling. Thêm `PageFooter` + hàm "mục dở đầu tiên" (Từ mới → Ghép câu → Truyện; đủ 3★ → "Luyện lại: …") |
| R20 | **TopicHub: hàng truyện trống và các dòng đếm** | `TopicHub.tsx:166-172` thẻ mờ chỉ có chip "Sắp có 📖", emoji không grayscale; `:143` "{words}/{deckSize} từ" là dòng phụ; `:155` "{starred}/{sentences.length} câu có sao" | thêm dòng "Đảo này chưa có truyện — **nghe truyện ở 3 đảo khác** nhé" 12px + `grayscale(1)`; đếm nằm **trong tiêu đề** ("Từ mới **3/8**"); hàng câu hiện **sao** thay dòng đếm | Áp; số "3 đảo" tính từ `STORIES` (`new Set(STORIES.map(s => s.topic)).size`), không hard-code |
| R21 | **LevelStairs: CTA chỉ có ở phone, iPad còn 5 margin ma thuật** | `LevelStairs.tsx:183` `PageFooter className="md:hidden"`; `:16-20` `ipad:mt-[240/180/120/60/0px]`; `:132` `ipad:grid-cols-5`; `md:` = lưới 2 cột ô `md:h-[180px]` (`:38-39`) | CTA **có ở iPad ngang** (420×64 căn giữa); iPad: `left = 10%+i·20%`, `top = 70%−i·17.5%`, ô **176×176**, SVG `1080×600`; **iPad dọc = zigzag phone, ô 300×96** | Bỏ `md:hidden` ở footer; thay 5 `mt-` bằng `style={{left,top}}` + `absolute` trong hộp `relative flex-1` (giống bản đồ Home `Home.tsx:451`); `md:` (dọc) dùng lại zigzag phone với ô `md:w-[300px] md:h-[96px]` thay lưới 2 cột — **xoá layout `md:` grid hiện tại** |
| R22 | **LevelStairs phone: vùng bậc cuộn riêng, tag/Foxy lệch cỡ** | `LevelStairs.tsx:132` `justify-around` (không `space-between`), zigzag nằm thẳng trong `PageBody` (cả trang cuộn); Foxy `h-16` (64) (`:154`); tag `text-[13px]` (`:165`) | vùng bậc `overflow-y:auto` riêng + `space-between`, "cuộn về đáy khi mount"; Foxy **58×56**; tag 12px | Bọc zigzag trong `div flex-1 min-h-0 overflow-y-auto` + `useEffect` scroll đáy khi mount; `justify-between`; áp cỡ. Ô 236×84 đã khớp (`:38`) |
| R23 | **StoryPlayer: dòng tên truyện, pill gợi ý, hai dòng trạng thái audio** | `StoryPlayer.tsx:59-62` khối tiêu đề `hidden … md:block` trong thân; `:71-73` pill "👆 Chạm vào 1 từ…" **absolute trên tranh** (`max-md:hidden`); `:104,108` 2 dòng `<p className="text-center text-sm … text-ink-300">` | tên truyện 11px **dưới chip trong header**; pill gợi ý → **dòng 13px trên karaoke** ở mọi frame; 2 trạng thái audio = **`Notice` 44px** (info / error + nút "Thử lại") | Chuyển tên truyện vào cụm giữa header (chip + dòng 11px); bỏ pill absolute; dùng `Notice kind="info"` và `kind="error"` + `action={{label:'Thử lại'}}` — cần hàm `retry()` trong `useStoryPlayer` (**hiện không có**) |
| R24 | **Karaoke đang giữ floor 64px/từ (Q11)** | `components/Karaoke.tsx:24` `min-h-[64px] min-w-[64px] px-2`, gap `gap-x-2` (8) | hit **44×44**, padding `8px 6px`, gap 4 → "9 từ × ≈52px vừa 2 dòng trong 358" | Bỏ `min-w-[64px]`, `min-h-[44px]`, `px-1.5 py-2`, `gap-x-1`. Ghi Ruling: đây là **ngoại lệ có chủ đích** với luật 64px của trẻ, đúng như Q11 phát biểu |
| R25 | **PlayerControls lệch vài số** | `components/PlayerControls.tsx:16` ⏮⏭ `rounded-2xl` (vuông bo) 64; `:17` chip tốc độ `h-10 w-11` (40×44); `:83` nhãn "Phụ đề Việt" | ⏮⏭ **tròn**; chip **44×40** radius 11; nhãn "**🇻🇳 Phụ đề**" | Áp. Play 96/104 và toggle 50×28 đã khớp. Q12: code vốn không có nhạc nền → **không việc**, chỉ xoá dòng chú thích "Q12 chưa chốt" ở `:73-76` |
| R26 | **StoryQuiz thẻ đáp án cố định 250×270, bố cục cột** | `StoryQuiz.tsx:175` `max-md:min-h-[96px] md:h-[270px] md:w-[250px]`, `flex-col`; emoji `text-[64px] md:text-[110px]` (`:177`) | phone: **hàng ngang** gap 18, emoji **56** + nhãn 20px; iPad: `flex:1` max **300**, tỉ lệ **4:3**, emoji **96** | `flex-row` phone (`md:flex-col`), `md:aspect-[4/3] md:max-w-[300px] md:h-auto md:w-auto md:flex-1`. Thêm nhánh `opt.image` (ảnh 16:9 thay emoji, Q14) tuy dữ liệu hiện chưa có |
| R27 | **StoryQuiz 0/3 vẫn 1★, và store không lưu được 0 sao** | `StoryQuiz.tsx:72` `stars = 3 \| 2 \| 1` (0 đúng vẫn 1); `:99` Foxy `cheer\|happy` (không có `idle`); `:114` primary luôn "Kể lại câu chuyện →"; `progress/store.ts:13` `setStars(id, stars: 1 \| 2 \| 3)` | 0 đúng → **0★ idle**, primary đổi "🎧 Nghe lại truyện", nút thứ 3 là **link chữ 44 gạch chân** | Thang sao thành `0\|1\|2\|3`; `setStars` phải nhận 0 **hoặc** màn không gọi `setStars` khi 0 *(đề xuất: không gọi — `setStars` chỉ tăng, ghi 0 là no-op; vẫn `logActivity`)*; `Stars value` đã nhận 0 (`Stars.tsx:8`) ✔; nút thứ 3 dùng `LinkText` (`components/ui/LinkText.tsx`) thay `Button` |
| R28 | **`LessonChip` và ô phải chia nhau header** | `PageHeader.tsx:17` ô phải mặc định `<LessonChip />`; `LessonChip.tsx:71-74` hộp 56×56 phone / pill cao 48 md — **khớp design**; Home đè bằng `right={parentButton}` (`Home.tsx:336`) | Ô phải = "LessonChip / ⭐ tổng sao / trống"; Home iPad dọc để **streak + ⭐ + phụ huynh** ở đó; C1/C8 vẽ **không có** ô phải | Không đổi `LessonChip`. Quy ước: màn nào cần ô phải khác thì truyền `right`; C1/C8/A10–A14 để mặc định (chip tự ẩn khi không phải bước nhiệm vụ, `LessonChip.tsx:86-88`) — **không** truyền `right={null}` (ô rỗng vẫn giữ chỗ 56/64, đúng lưới) |
| R29 | **`short:` 375×667** | variant đã có (`client/tailwind.config.ts:150`); dùng ở `StoryPlayer.tsx:91` | Design chỉ ghi: C6 ô **109** vẫn vừa; A9 bậc **72px** vẫn vừa; phụ đề mặc định tắt <700 cao | Không thêm luật `short:` mới cho danh sách (ô tự co theo `1fr`). A9: `short:h-[72px]` cho ô bậc. Phụ đề: `useStoryPlayer` đọc `window.innerHeight < 700` khi mount *(đề xuất)* |
| R30 | **`PageBody` không có `gap` mặc định** | biến thể thường là `flex flex-col` trần (`components/ui/page/PageBody.tsx:73`); mỗi màn tự vá bằng `mt-2.5`/`mt-3`/`gap-*` truyền qua `className` (`DailyMission.tsx:131,136`, `LevelStairs.tsx:132`, `Home.tsx:360`, `TopicHub.tsx:117`) | vùng cuộn có `gap` cố định: **10** (lưới ô) / **8** (danh sách hàng) / 12 iPad | `PageBody` nhận `gap?: 8\|10\|12` (mặc định giữ 0 để 33 màn không đổi); màn danh sách truyền qua `ListGrid`/`StickyGroup` thay vì `mt-*` rải rác |
| R31 | **Thang cỡ sao thiếu 2 mốc** | `components/ui/Stars.tsx:2` `xs 12 / sm 16 / md 28 / lg 44` | design dùng **11** (đảo phone), **12** (bậc phone), **13** (ô, hàng, đảo iPad-ngang cũ), **14** (đảo iPad dọc, bậc iPad), **44** (kết quả quiz) | Thêm mốc **13** và **14** vào `SIZE` (`sm2`/`md2`? *(đề xuất: đổi thang thành số — `size={11\|12\|13\|14\|16\|28\|44}`)*), hoặc chấp nhận lệch 1px và dùng `xs` (12) cho 11/12/13 · `sm` (16) cho 14. Chọn dứt khoát một lần: 5 màn cùng đọc bảng này |
| R32 | **Design không ghi** | — | iPad cho C1/C8/C5/A10–A14/A7/A8/C2/C3 (chỉ ghi chú, không artboard); `?topic=` của C8; empty của C1/C5; loading/`Skeleton` ở mọi màn danh sách; ô phải của C1/C8 | Suy từ C6 (màn tham chiếu 3 frame): iPad dọc = phone với số cột 5/3 và cỡ ×1.25; iPad ngang = iPad dọc với 6/4 cột trong `max-width:1080`. `Skeleton` giữ nguyên Phase 12 |

---

## 5. Rủi ro (những gì Phase 12/13 vừa dựng có thể vỡ)

1. **`PageHeader` đổi từ căn giữa sang căn trái (R2)** là thay đổi ảnh hưởng **cả 33 màn**, trong đó 9 màn luyện nói vừa chốt ở Phase 13 dùng cụm giữa cho chip/`ChipPair`/"● Đang ghi". Phải thêm `align` **có mặc định là hành vi cũ**, không đổi mặc định.
2. **`ipad:` outrank `md:`** (Phase 13) khiến mọi `md:` mới viết cho iPad dọc trong vòng này (R13, R17, R21) **không** tự động áp ở iPad ngang — phải kiểm cả hai frame cho từng màn, đúng như bài học `md:flex-row` > `ipad:flex-col`.
3. **`PageFooter` mới ở TopicHub/DailyMission-rỗng/LevelStairs-iPad (R16, R19, R21)** ăn chiều cao của `PageBody`; những màn này chưa từng có footer → phải đo lại overflow bằng `shoot.mjs` (probe đọc `PageBody`).
4. **`PageHeader.onBand` của TopicHub (R19)** là ngoại lệ đầu tiên với "header luôn trên cream" — nếu làm bằng `className` đè thì `dimmed` và `EngineBadge` phải vẫn đúng.
5. **Xoá `CARD_LINK` (R1)** đụng 7 file; test hiện có (`WordList.test.tsx`, `StoryList.test.tsx`, `LevelSelect.test.tsx`, `PairLevel.test.tsx`, `StarLevel.test.tsx`, `VoiceLevel.test.tsx`, `WordTopics.test.tsx`) chủ yếu kiểm số ô + đích link → nên sống sót; kiểm lại các assert đếm `grid-cols-*`.
6. **Đổi mốc `HomeLabel` từ `md:` sang `ipad:` (R14)** đổi **chuỗi đọc được** ở 6 call-site → cập nhật test dựa vào `getByRole('link', { name: 'Về bản đồ 🏝️' })`.
7. **`setStars` nhận 0 (R27)** là thay đổi kiểu lan ra toàn bộ `progress/store` — đề xuất **không** đổi kiểu, chỉ không gọi.
8. **Kết quả quiz 0/3 không chụp được bằng kịch bản hiện có** — `shoot.mjs:210` `quiz-result` trả lời **đúng** cả 3 câu. Muốn ảnh 0★ phải chọn sai rồi chọn đúng 3 lần (được: sai không tự chuyển, `StoryQuiz.tsx:88-92`), hoặc thêm `?fixture=` như Phase 13 đã làm cho `result3`. Chọn cách kịch bản — màn này không có ngẫu nhiên.
9. **`words-review` 64 ô chưa từng nằm trong `shoot.mjs`** (deck ôn tập rỗng theo seed mặc định; con số 7028px trong README Phase 12 đến từ script một lần). Vòng này bắt buộc phải seed nó, nếu không thì trường hợp xấu nhất của cả khung danh sách không bao giờ được đo.
10. Không đụng `useSpeakingAttempt`, `MicButton`, `ResultCard`, `PageBody.split` — vòng này không chạm màn luyện nói (trừ B2 hạ cỡ ô).

---

## 6. Tóm tắt việc mới (Phase 14)

**Component mới:** `ListGrid` · `Tile` (ô nhỏ / ô lớn / khoá / accent) · `ListRow` (64 / 96) · `StickyGroup` · `EmptyState size='hero'` · `Button size='sm'` (48) · `PageHeader` `title`/`sub`/`align`/`onBand` · `PageBody.fade` · `Chip` tone coral đặc.
**Component sửa:** `Karaoke` (hit 44) · `PlayerControls` (⏮⏭ tròn, chip 44×40, nhãn) · `MissionCard` (128 cố định, 4 trạng thái) · `NoticeStack` (dòng "+N" thành nút nêu tên) · `HomeLabel`/`BackButton.mdLabel` (`md:` → `ipad:`) · xoá `CARD_LINK`.
**Màn (3 frame):** C6, C1, C8, C5, A10, A11, A12, A13, A14, B2 (chỉ cỡ ô) · A3 (iPad dọc + phone 3 banner) · A6 (rỗng + iPad ngang) · A7 (0 sao) · A8 (dải + CTA ghim) · A9 (phone cuộn + iPad %) · C2 · C3 (hỏi + 0/3).
**Logic:** nhóm review theo chủ đề (R6) · "mục dở đầu tiên" của TopicHub (R19) · thang sao quiz 0–3 (R27) · `retry()` cho audio truyện (R23) · phụ đề mặc định theo chiều cao (R29) · đếm "3 đảo khác" từ dữ liệu (R20).
**Tooling:** `shoot.mjs` thêm `words-review` (seed 64 từ đến hạn), `stories`, `sentences-topic`, `mission-empty`, `mission-done-zero`, `topic-no-story` (`/topic/weather`), `quiz-result-zero`, `story-player-no-audio`, `home-3-banners`, `levels` ở `ipadp`; chạy đủ 3 frame + `VIEWPORTS=short` cho C6/A9; so `docs/design/current-phase13/shots` → `current-phase14`. Mốc phải hạ: `phone/words-animals-full.png`, `phone/words-full.png`, `phone/sentences-full.png`, `phone/level-*-full.png` (5 file), `ipad/sentences-full.png`, `ipadp/sentences-full.png`.
