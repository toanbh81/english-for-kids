# Phase 15 — Khu người lớn (vòng 4): một mốc chạm 44, ba cổng một ngôn ngữ, dashboard 10 panel

Phase 12 dựng khung trang và bộ component người lớn (`Button size='adult'` 44, `BackButton variant='adult'`, `Notice`/`NoticeStack` có `adult`, `Dialog`/`DialogProvider`/`useDialog`, `SyncPill`, `Skeleton`/`AccountCardSkeleton`, `EmptyState adult`, `LinkText`), Phase 13 dựng khung luyện nói và luật `ipad:` thắng `md:`, Phase 14 dựng khung danh sách cùng `PageHeader title/sub/align`, `PageBody fade/gap`, `Chip` cỡ `xs`. Phase 15 **không đụng màn trẻ em**: nó gom **bốn màn người lớn** (A1 ProfileGate, A2 CloudStart, P1 ParentGate, P2 ParentDashboard) về **một ngôn ngữ** — một thẻ cổng, một mốc chạm, một lưới panel — thay 4 chỗ tự dựng `Card max-w-md`, 9 chỗ lặp `Card px-4 py-3.5 md:p-6`, và hạ ba mốc cuộn của dashboard.

**Số đo nằm trong brief, không ở đây:** `docs/design/2026-09-04-round4-parent-zone-brief.md` (§0 năm quyết định gốc, §1 số đo theo 3 frame — thẻ cổng và lưới panel, §2 bốn màn + 11 trạng thái thẻ Tài khoản + 7 trạng thái Tiến độ từ xa + 14 câu lỗi + 4 dialog, §3 Q17/Q18, §4 R1–R31, §5 mười rủi ro, §6 việc mới). Spec này chỉ ghi quyết định đã chốt và luật ràng buộc.

## Quyết định (người dùng duyệt 2026-09-04: "làm theo đề xuất" cho R1–R31)

**Luật người lớn**

1. **Ruling "hộp thấy 28–36, vùng chạm ≥44"** (R3, R29) — **đảo chiều** đoạn doc comment "36–48" ở `ParentDashboard.tsx:47-56` và comment "the 64 px row is the child floor" ở `:877-881`. Trong nhóm A1/A2/P1/P2: hộp thấy được là **28 / 32 / 36 / 44**, mọi hộp <44 phải mang dải hit (`after:-inset-*`, mẫu `Button` `HIT` và `Notice.tsx:10` `CHILD_HIT_BAND`); **không nút trẻ em 56/64 ở bất kỳ đâu trong nhóm** — xoá cả 10 chỗ `md:min-h-[64px]`/`md:h-16`/`min-h-[64px]` (`ParentDashboard.tsx:883,903,929,962,979,999`, `CloudStart.tsx:432,521,539`, `ProfilePicker.tsx:95`) và `Button` mặc định md ở `ParentQuestion.tsx:69`. Chữ người lớn: **thân 13–14px**, phụ **12px**, help 11px, tiêu đề thẻ/dialog 18px, H1 dashboard 20/24. Ruling này phải nằm trong spec **trước** khi sửa, nếu không lần review sau sẽ khôi phục đúng những dòng vừa xoá (rủi ro 1).

**Component dùng chung — mới**

2. **`GateCard`** (R1): trắng r20 padding 20, `w-[min(420px,calc(100%-32px))]`, gap 12, `0 6px 0 #EFE2CC`, **căn trái** — dùng **đúng token của `Dialog.tsx:84`** (cùng thẻ, khác chỗ đặt). Bốn call-site bỏ `max-w-md` (448) và `text-center`. Thẻ A2 thống nhất **padding 20** (design vẽ 18, đề xuất 20 được duyệt).
3. **`Panel`** (R13): trắng **r16** padding `12px 14px` (phone) / `14px 16px` (iPad), `0 4px 0 #EFE2CC`, `title` **13/14px 800**, `right?`, `collapsible?` (hàng gập `min-h-[56px]` + `▾`/`▸`), vùng cuộn `flex-1 min-h-0` + fade đáy 40. Không đè `Card` từ ngoài — `Card` là `rounded-xl3`, Panel là component riêng (rủi ro 6).
4. **`PanelGrid`** (R12): `grid-cols-1 md:grid-cols-2 ipad:grid-cols-3` + `Panel col='full'`. **Một cây DOM duy nhất**, thứ tự DOM = **thứ tự phone**; hai frame kia chỉ đổi số cột. Panel muốn "cột 3 ở iPad ngang" phải chấp nhận vị trí của nó ở phone (rủi ro 4).
5. **`FieldRow`** (R9): `label` 12px 800 **trên** ô · `input` **h44** r12 viền 2px (`#E2D5C0` / teal focus / `#C2354B` lỗi, `truncate`) · `error` 12px `min-h-[18px]` **dưới ô, giữ chỗ sẵn** (không đẩy form), nhận `action?` cho nút "Thử lại" 44 trong dải lỗi · `help` 11px dưới cùng. `Notice` chỉ còn dùng cho thông báo cấp màn.
6. **`SegRow` + `Stepper`** (R23, R24): seg `flex-1 h44 r12` 13px, tone **`on` / `off` / `dim`** (`#EFE2CC`/`#8A7A6D` + `2px dashed #D9CBB4`); `Stepper` −/+ **36×36** r10 sand trong hit 44 + ô **64×36** (phone 56×36) viền teal, bước 5, dải 5–60. Bỏ `<input type="number">` (bàn phím số iOS che màn), giữ input ẩn cho a11y.
7. **`MinutesChart`** (R19, Q17): plot h120 (phone h86) gap 6/9, cột `flex-1 max-w-[26px]` **r7** `max(4, v/max*100)%`; **4 màu** — hôm nay coral · `v≥20` teal · `v>0` sun · `0` `line-200` cao 4; đường mục tiêu nét đứt theo giới hạn đang đặt + nhãn "mục tiêu 20'"; **3 nhãn mốc** (`days[0]` · `days[7]` · "hôm nay") thay 14 nhãn; nút đổi "7 · 14" trong tiêu đề (chỉ iPad).
8. **`RecordingRow`** (R22, Q18): hàng h44, ▶ **36** tròn teal (hit 44), ngày 11px, câu 13px **1 dòng ellipsis**, điểm 11px màu băng (`≥80` good · `50–79` sun · `<50` fix). **Kiểm trước:** `progress/recordings.ts` có trường `score` chưa — nếu chưa thì thêm khi ghi; nếu không thể thì bỏ cột điểm và ghi rõ, không hứa suông (rủi ro 9).
9. **`RemoteRow`** (R18): hàng `min-h-[56px]` (56–72), 🦊 20 · tên 13px ellipsis · **một chuỗi phụ 11px ellipsis** nén cả 3 dòng số ("🔥 4 ngày · 58'/tuần · Nói 79 · Từ 77 · Câu 70 · Âm sai /θ/ 46") · nút hàng h36 ("Chi tiết" / "Thử lại").
10. **`AccountCard`** (R15): **11 trạng thái** §2, `min-h-[150px]` giữ cao, mỗi trạng thái một pill sync riêng. Tách ③ offline khỏi ② (`noSessionNotice()` `:110` không còn gộp); thêm ⑩ sync lỗi (đọc `syncStatus().pending` + `lastError`) và ⑪ đang đăng xuất ("Đang lưu 3 mục còn lại trước khi đăng xuất…"); ⑤ có **spinner 16 trong nút**, không chỉ `disabled`.
11. **`Button variant='danger'`** (R14): outline đỏ nhạt — viền `2px #F8A3AE`, chữ `#C2354B`, nền trắng. Là **variant thật**, không `className` đè (rủi ro 6: base class thắng `className`).

**Component sửa — chỉ bảy cái này**

12. **`ProfilePicker`** (R6): 3 mật độ tự suy từ `profiles.length` — **1** không hiện · **2–3** một hàng ô `flex-1` cao **96** · **4–8** lưới `grid-cols-2 md:grid-cols-4` ô **88**, vùng cuộn `max-h-[380px]` + fade 36; ô nhỏ CloudStart ⑦ cao **72**. Tên **14px `line-clamp-2` + `title` đầy đủ** (không cắt ở tầng dữ liệu), dòng phân biệt 11px; `pendingId` = spinner **ở đúng ô đang chọn**, lưới mờ .5; ✓ góc ô active. **Bỏ `sm:`** (mốc thứ 4 ngoài 3 frame). `density` có **mặc định = hành vi cũ**; đụng cả 3 call-site (`ProfileGate.tsx:169`, `CloudStart.tsx:371`, `ParentDashboard.tsx:675`) và `ProfilePicker.test.tsx` (rủi ro 2).
13. **`ParentQuestion`** (R4): phép tính **32px** (không 44), ô **96×44** r12, nút "Vào" 44 **phải**, tiêu đề 18 + `sub` mới (P1 và A2 ② khác câu); dải lỗi 12px `min-h-[18px]` **tự tắt khi bắt đầu gõ**; **rung 300ms** khi sai (`animate-shake` — thêm keyframe vào `tailwind.config.ts`).
14. **`SyncPill`** (R16): **7 trạng thái** — ✓ Đã đồng bộ · ✓ Đã đồng bộ · HH:MM (gộp "🕘 lúc" vào đây, nền `good-50`) · ● Chưa đồng bộ N mục · ◌ Đang đồng bộ… · ⚡ Ngoại tuyến · **⚡ Chưa kết nối** · ⚠ Không đồng bộ được. Nhánh mới gate bằng **`hasSession === false`, không phải `state === 'off'`** (`off` = cloud chưa cấu hình → vẫn `return null`); `ParentDashboard.tsx:545` phải nối thêm `hasSession` (rủi ro 3). Prop `size='sm'|'md'` = **h28 r8 11px** (trong panel hẹp) / **h32 r10 12px** (header panel Tài khoản).
15. **`Notice icon?`** (R26) đè icon mặc định mà giữ tone (③ dùng warn + **📡**); **`EmptyState variant='dashed'`** (hộp h120 viền `2px dashed #E2D5C0` r12 cho biểu đồ rỗng — "không vẽ 14 cột 2%"); **`AccountCardSkeleton` h-[150px]** (từ 168, R28), không thêm skeleton mới. Giữ `Notice kind='credential'` cho mã khôi phục ⑧ (khung teal đọc rõ hơn artboard) — chốt dứt khoát một lần.
16. **`Dialog` chỉ thêm `placeholder?`** (R27, rủi ro 8): `Dialog.tsx` đã đúng từng con số của design; `DialogProvider`/`useDialog` **không đụng một dòng nào**. Mọi thay đổi khác ở 3 file này là hồi quy so với Phase 12.

**Ba cổng, một ngôn ngữ**

17. **Khung chung 3 cổng** (R1, R2): `PageShell` + `PageHeader` + `PageBody` căn giữa dọc ở **cả 3 frame** (iPad không chia 2 cột — nội dung ngắn), nền blob mờ (`#F3EADA` 280 phone / 420 + `#E2F6F1` 360 iPad). Back 44 có nhãn **ngoài thẻ, góc trên trái**: P1/A2 thêm `mdLabel="Về bản đồ 🏝️"` qua `BackButton.tsx:50-55` (`ipad:` đã hỗ trợ) — **không thêm mốc `md:` mới**. **A1 giữ không có Back** (cổng chọn hồ sơ không có đường ra — đúng chủ ý cũ).
18. **A1 ProfileGate hai hình dạng** (R5): ② mở lạnh ≥2 hồ sơ = **màn toàn màn hình** (thẻ giữa, nền cream + blob, không app phía sau); ④ quay lại ≥5 phút = **overlay** `fixed inset-0 z-40 bg-[rgba(74,59,51,.45)]`, app phía sau bọc một div `blur-[2px] opacity-60`. **Thang z sau vòng này: overlay 40 < Toast 50 < Dialog 60** — hôm nay `ProfileGate.tsx:190` là `z-50` bọc `PageShell` đặc, ngang `Toast.tsx:9`.
19. **A1 sáu trạng thái** (R6, R7): ① 0–1 hồ sơ không hiện · ③ đã chọn <5 phút bỏ qua · ⑤ chạm hồ sơ active đóng ngay, hồ sơ khác → ô busy spinner rồi reload · ⑥ **storage lỗi** → `writeMark` trả `boolean`, state `storageBroken` → `Notice kind="info" adult` **một dòng 12px** "Không nhớ được lựa chọn — sẽ hỏi lại lần sau" trong thẻ, **không đổi hành vi hỏi lại**. Xấu nhất: 8 hồ sơ (5 tên "Bé" trùng, 1 tên 29 ký tự, 3 kiểu dòng phân biệt) = 4 hàng × 88 + 3 × gap 8 = **376 ≤ 380**, hồ sơ thứ 9 mới cuộn.
20. **P1 ParentGate ba trạng thái** (R4): sạch · **sai** (đổi câu hỏi + rung + dải lỗi "⛔ Chưa đúng — câu hỏi đã đổi, thử lại nhé.") · **gửi rỗng** = câu riêng "Nhập kết quả trước nhé" và **giữ nguyên câu hỏi** (hôm nay `ParentQuestion.tsx:42-49` coi rỗng là sai). Không khoá sau N lần — giữ chủ ý cũ.
21. **A2 CloudStart tám stage** (R8): thêm `'result'` vào `Stage` (hôm nay 6 giá trị, `CloudStart.tsx:37`); ⑧ = `Notice warn` "Tài khoản này chưa có hồ sơ nào để khôi phục…" + outline "Thử tải lại" + primary "Bắt đầu mới cho bé" + link "← Về menu". `info`/`retryId` chuyển **vào thân stage ⑧** thay vì rắc lên đầu mọi thẻ (`:388-389`, `:393`). Mọi ô nhập hạ **44** và đi qua `FieldRow`; ô OTP/mã khôi phục Baloo 22px `tracking-[6px]` căn giữa.
22. **A2 mười bốn câu lỗi + gộp lỗi hệ thống** (R10): áp **nguyên văn** 14 câu §2 vào `describeAuthError` (7) và `describeRecoverError` (7) — giữ **khoá mã ổn định** để `CloudStart.test.tsx` bám mã chứ không bám chuỗi (rủi ro 7). Năm lỗi hệ thống rải ở `:204,:218,:333,:341,:356` gộp còn **một hằng** "Không kết nối được máy chủ — thử lại sau" + `action` "Thử lại" 44 **trong dải lỗi**, mã **không bị tiêu** — **trừ `:333` (roster không đọc được), giữ câu riêng** vì hậu quả khác hẳn: gộp 4, không gộp 5.
23. **A2 abandon** (R11): 4 dòng copy 12px nền `sun-100` r10 padding `8px 10` (có số · có số + đã lưu · 0 sao/0 lượt · chưa kiểm tra được — 4 nhánh hiện có đã đủ, chỉ đổi khung trình bày); nút primary nhãn **cố định "Vẫn tiếp tục với email này"** — **email 61 ký tự không được nhét vào nhãn nút** (`:504` hôm nay + `whitespace-nowrap`), email đã hiện ở dòng copy; thêm ghost "Huỷ" và link "Sao lưu trước ở Góc phụ huynh".

**P2 Dashboard**

24. **Lưới 1/2/3 với cả 10 panel bên trong** (R12, R30): phone 1 cột gap 10 · iPad dọc **Tài khoản full-width có 2 cột trong** (`minmax(0,1fr) minmax(0,1fr)` gap 16, cột phải `border-left 2px line-200 pl-4`) rồi lưới 2 cột · iPad ngang **3 cột trong `max-width:1080`**. **Tiến độ từ xa full-width, hàng áp chót**; hàng "Đặt lại" cuối cùng. Hôm nay `:776` là 1/1/2 và **3 panel nằm ngoài lưới** (`:542`, `:714`, `:1017`).
25. **Header một hàng căn trái** (R25): dùng `PageHeader title/sub` **có sẵn từ Phase 14 — không thêm prop mới**; H1 20/24 + phụ 12/13 **chính là dòng tóm tắt** hiện đang nằm trong thân (`:537`); sub rỗng → "Chưa có buổi luyện nào tuần này". Nút "🔐 Khoá lại" **tự vẽ** 44 nền `#F3EADA` chữ `#A79781` (phone icon-only 44×44 r12) — `Button` không có variant sand, không đẻ thêm.
26. **Panel Tài khoản + Hồ sơ** (R15, R6, R17): Tài khoản = `AccountCard` 11 trạng thái; Hồ sơ = cột phải, "+ Thêm hồ sơ" h32 + hàng `min-h-[40px]` (🦊 20 · tên 13px **1 dòng ellipsis** · sub 11px · "Đổi tên" h32 gạch chân), xấu nhất **8 hồ sơ = 320**. **Email 61 ký tự: `truncate` + `title={email}` + `min-w-0` ở cha `flex`** tại cả 3 chỗ (`:588`, `:627`, thẻ gập phone) — theo artboard vẽ `ellipsis`, không `break-all` như thẻ mở đầu ghi.
27. **Biểu đồ phút luyện** (R19, Q17): `MinutesChart`; **14 ngày mặc định ở `md:`/`ipad:`, 7 ngày ở phone** — chia này **đã đúng trong code** (`:61`, `:816`), việc mới là nút đổi, 3 nhãn mốc, 4 màu, `max-w-[26px]`/r7/gap 6, `max(4,…)` thay `max(2,…)`; nút đổi **ẩn ở phone**; rỗng = `EmptyState variant='dashed'`.
28. **Điểm trung bình + Âm hay sai** (R20, R21): điểm TB vào `Panel` như 9 panel còn lại (H2 hôm nay nằm **ngoài** `Card`, `:832-834`), **4 ô** `grid-cols-4` thêm `'story'` — **kiểm trước** `averageScoreByKind('story')` có dữ liệu không; nếu event `story` không bao giờ có `score` thì ô luôn "—" và **đó đúng như artboard vẽ** (rủi ro 9). Âm sai: chip **h36 r12 `nowrap`** "/θ/ · 46 (10 lần)", tone `<50` fix / `50–70` sun; **phone: chip là `<button>` toggle tip** (hôm nay `:868` `hidden md:block` — phone không có đường nào xem tip); xấu nhất 5 chip + tip 3 dòng.
29. **Giới hạn + Bài học** (R23, R24): giới hạn = `SegRow` 4 seg (15'/20'/30'/**"Tuỳ chỉnh {n}'" sáng khi giá trị ∉ {15,20,30}**) + `Stepper` + "Hôm nay: 12/25'" bên phải tiêu đề. Bài học = 6 seg một hàng ("Tự động" + 1–5), auto on → **bậc hiện tại tone `dim`, không sáng cùng** (hôm nay `:962`/`:979` sáng đồng thời) + dòng 11px "Tự động đang chọn → bậc hiện tại ⭐ 2"; đổi nhãn "Thời lượng" → "**Độ dài nhiệm vụ**" và 3 chuỗi "Ngắn ~8'"/"Vừa ~12'"/"Dài ~18'"; chân "Áp dụng từ bài học ngày mai."; `progress/band.ts` **không đổi**. Phone: cả hai panel gập thành hàng 56.
30. **Bản ghi gần đây · 20** (R22, Q18): mặc định **5 hàng + "Xem tất cả 20 bản ghi ▾" mở tại chỗ (không dialog)**; đang phát ▶ → ❚❚ + **thanh 3px dưới hàng**; phát lỗi → **hàng đỏ nhạt + "Không phát được"** (hôm nay `:902` `.catch(() => {})` im lặng); 0 bản ghi → `EmptyState` 🎙️. Hàng gập phone xuống **56** (bỏ `min-h-[64px]` `:883` theo quyết định 1); `recordingsOpen` **giữ nguyên cách quyết định** (`matchMedia` một lần khi mount, `:172`).
31. **Tiến độ từ xa 7 trạng thái** (R18): đang tải · lỗi tải (+ nút "Thử lại" — hôm nay chỉ có câu) · chưa có · có dữ liệu · **máy này** ("Bé · máy này", ngắn hơn " · đang dùng trên máy này") · **cũ >7 ngày** (`Date.now() - updatedAt > 7 ngày`, "Cập nhật 12 ngày trước · 🔥 0 · 0'/tuần") · không sync bản ghi. Chú thích "bản ghi không đồng bộ" ghi **một lần dưới panel**, không lặp mỗi hàng (`:716` đã đúng). "Chi tiết" mở `Dialog`.
32. **Hàng "Đặt lại tiến trình"** (R14): riêng một hàng **cuối cùng**, `mt-6 flex items-center justify-between gap-3` — mô tả 12px trái ("Xoá sao, chuỗi ngày và bản ghi trên máy này (và trên tài khoản nếu đã liên kết).") + `Button size='adult' variant='danger'` nhãn "↺ Đặt lại tiến trình…" phải (hôm nay là `outline` = trắng/teal, nằm trong `flex-col gap-2` chung).

**Dialog và câu hỏi mở**

33. **Năm dialog, chỉ đổi chuỗi ở call-site** (R27): xoá·chưa liên kết = "**Xoá tiến trình trên máy này?**" / nút "Xoá trên máy này"; xoá·đã liên kết = "**Xoá toàn bộ tiến trình của bé?**" / nút "Xoá tất cả" — **hai tiêu đề khác nhau**, hôm nay `:392` dùng chung một tiêu đề và chỉ đổi `body`; đăng xuất = "Đăng xuất tài khoản?" + thân 3 câu có "3 mục chưa đồng bộ sẽ được gửi trước", nút **coral**; thêm hồ sơ = "Thêm hồ sơ **mới**" + placeholder "**Ví dụ: Bé Su**" + đếm "0/40"; **đổi tên** (không có artboard) theo đúng mẫu "Thêm hồ sơ mới", giữ `initial`.
34. **Q17 đã đúng, Q18 giữ nguyên** (§3): Q17 = 14 ngày iPad / 7 ngày phone — **`PHONE_DAYS = 7` trong code đã là câu trả lời**, chỉ thiếu phần trình bày (quyết định 27). Q18 = **giữ "Bản ghi gần đây" cả ở phone**, giữ "Điểm trung bình" (thêm cột Truyện), giữ ô phút tuỳ chỉnh, giữ dòng "Áp dụng từ bài học ngày mai."
35. **Design không nói — 5 ruling** (R31): **Dashboard iPad ngang = 3 cột** trong `max-width:1080` (`PageShell.tsx:13` đã có), suy từ đoạn ghi chú của thẻ mở đầu, không phải từ artboard · **dialog đổi tên** theo mẫu "Thêm hồ sơ mới" · **`cloudAvailable === false`** ẩn Tài khoản/Hồ sơ/Tiến độ từ xa → còn **6 panel**, lưới 1/2/3 giữ nguyên · **ParentGate/CloudStart iPad dọc = thẻ phone 420 căn giữa** (như iPad ngang, theo luật `ipad:` của R14 vòng 3, nhãn Back "Về nhà") · **ProfileGate 1 hồ sơ = không hiện, 3 hồ sơ = một hàng ô 96**. `short:` 375×667 **không thêm luật mới** (thẻ cổng đã `min(420,100%−32)`, dashboard đã cuộn).

## Luật ràng buộc
- Số đo **nguyên văn** từ brief §1–§2; token/bóng/radius Phase 12 và mọi thứ Phase 13/14 vừa chốt giữ nguyên — **không hex nào mới**; ngoại lệ duy nhất: `#FFF6E0` (nền tip âm sai) chưa có token → dùng `sun-50` hoặc hex thẳng, brief đã ghi rõ là chỗ duy nhất không có token.
- Ba frame kiểm chứng: 390×844, 834×1194, 1194×834 (**+375×667 cho cả ba cổng và dashboard**); chụp bằng `docs/design/current/shoot.mjs` vào `docs/design/current-phase15/`.
- Dữ liệu xấu nhất phải vừa hoặc cuộn **không tràn `PageBody`**: tên **29 ký tự** · email **61 ký tự** · **8 hồ sơ trùng tên "Bé"** · **20 bản ghi** (câu 61 ký tự) · giới hạn tuỳ chỉnh **25'** · 5 chip âm sai + tip 3 dòng · 7 hàng Tiến độ từ xa. Ba mốc phải hạ: `phone/parent-dashboard-full.png` **1821 → ≈1100** · `ipad/parent-dashboard-full.png` **1643 → ≤834** · `ipadp/parent-dashboard-full.png` (mới) **≤1194**.
- **Không `lg:` và không `sm:`** ở bất kỳ file nào bị đụng (`ProfilePicker.tsx:83` `sm:grid-cols-3` là mốc thứ 4 ngoài 3 frame — xoá). `ipad:` thắng `md:` (Phase 13) ⇒ mọi `md:` viết cho iPad **dọc** vòng này (lưới 2 cột, panel 14/16, chart 14 ngày, picker 4 cột) phải kiểm **cả hai** frame iPad. Base class thắng `className` đè (Phase 14) ⇒ `SegRow`/`Stepper`/chip âm sai/`Button danger` là **variant thật**, không đè từ ngoài.
- Giữ mọi `data-testid` hiện có (`page-body`, `account-card`, `sync-pill`…); **không đụng `cloud/auth.ts`, `cloud/sync.ts`, `cloud/profileState.ts`, `progress/*`, các hook `useCloud*`** — trừ **ba thay đổi được phép**: (1) thêm `'result'` vào `Stage` và chuyển `info`/`retryId` vào thân stage đó trong `CloudStart.tsx` (R8), (2) gộp 4 câu lỗi hệ thống thành 1 hằng + `action` "Thử lại" trong `CloudStart.tsx` (R10), (3) `writeMark` của `ProfileGate` trả `boolean` (R7). Nếu R20/R22 buộc thêm trường (`score` cho `Recording`, `score` cho event `story`) thì đó là thay đổi **thứ tư**, phải ghi Ruling riêng trước khi làm.
- **`Dialog`/`DialogProvider`/`useDialog` chỉ được thêm `placeholder?`** — không gì khác.
- Tests/lint/typecheck/build xanh, **0 act()**; hook secret **không bỏ qua**; toàn bộ copy người lớn giữ **tiếng Việt**.

## Phạm vi
**Làm:** 11 component mới + 7 component sửa (quyết định 2–16) · 3 cổng (17–23) · dashboard 10 panel (24–32) · 5 dialog (33) · 3 ruling logic (`'result'`, gộp lỗi hệ thống, `storageBroken`) · `animate-shake` trong `tailwind.config.ts` · `shoot.mjs` 15 kịch bản mới + bản `-full` cho `ipadp/parent-dashboard` · ảnh trước/sau.
**Không làm:** mọi màn trẻ em (9 màn luyện nói xong ở Phase 13, 10 màn danh sách + Home + bản đồ xong ở Phase 14) · ảnh `art/` · các mục Phase 14 đã hoãn (`useCountdown`/`useTeachCollapse` — Phase 15 không đụng màn luyện nói nên **vẫn hoãn**) · mọi thay đổi luồng Supabase/auth/sync (`cloud/*` chỉ đọc) · Toast trong khu người lớn · trạng thái "đã hết giờ hôm nay".

## Kiến trúc
- **Mới** `client/src/components/adult/`: `Panel.tsx`, `PanelGrid.tsx`, `FieldRow.tsx`, `SegRow.tsx`, `Stepper.tsx`, `MinutesChart.tsx`, `RecordingRow.tsx`, `RemoteRow.tsx`, `AccountCard.tsx`, `index.ts`. `GateCard.tsx` vào `components/ui/` (dùng chung với `Dialog`, không riêng khu người lớn).
- **Sửa khung:** `ui/Button.tsx` (`variant='danger'`), `ui/SyncPill.tsx` (7 trạng thái + `size`), `ui/Notice.tsx` (`icon?`), `ui/EmptyState.tsx` (`variant='dashed'`), `ui/Skeleton.tsx` (`AccountCardSkeleton` 150), `ui/Dialog.tsx` (`placeholder?` — chỉ vậy), `components/ProfilePicker.tsx` (`density`, `pendingId`, `line-clamp-2`, bỏ `sm:`), `components/ParentQuestion.tsx` (viết lại theo §2 P1, thêm `sub`), `tailwind.config.ts` (`animate-shake`).
- **Màn:** `ProfileGate.tsx` (2 hình dạng + z 40 + `storageBroken`), `ParentGate.tsx` (`GateCard` + Back `mdLabel`), `CloudStart.tsx` (8 stage, `FieldRow`, 14 câu lỗi, abandon), `ParentDashboard.tsx` (`PanelGrid` + 10 `Panel` + header `title/sub` + hàng reset) — giữ hook/dữ liệu, đổi thân.
- **`shoot.mjs`** thêm: `profile-gate-8` (seed 8 hồ sơ, 5 trùng "Bé", 1 tên 29 ký tự), `profile-gate-reask` (`sessionStorage` mark 6 phút + `visibilitychange`), `parent-gate-empty`, `start-otp-error`, `start-code`, `start-abandon`, `start-result-empty`, `parent-dashboard-linked` (email 61 ký tự), `parent-dashboard-otp`, `parent-dashboard-sync-error`, `parent-dashboard-recordings-20`, `parent-dashboard-limit-custom` (`speakup.limit = 25`), `parent-dashboard-band-auto`, `parent-dashboard-empty`, `parent-remote-7` (stub `fetchRemoteStats`); thêm `parent-dashboard-full` vào `IPADP_ONLY`.

## Kiểm chứng
- Mỗi task: tests + lint + typecheck; chụp màn liên quan ở 3 frame (+`VIEWPORTS=short` cho 3 cổng và dashboard); probe `PageBody` phải hạ ba mốc ở "Luật ràng buộc".
- Cuối phase: sheet trước/sau (`current-phase14/shots` → `current-phase15`); README "Phase 15"; checklist iPad 6 hàng: **mọi control người lớn chạm được ở 44** (đo bằng ngón, không bằng mắt — hộp 28/32/36 vẫn phải bắt) · **thẻ cổng căn giữa ở cả 3 frame** (+375×667), không lệch trái · **dashboard đúng 1/2/3 cột** theo frame và Tài khoản có 2 cột trong ở iPad dọc · **thẻ Tài khoản qua đủ 11 trạng thái** (ép từng nhánh bằng kịch bản `shoot.mjs`) · **`SyncPill` 7 trạng thái, "⚡ Chưa kết nối" không hiện khi cloud chưa cấu hình** · **hai dialog xoá có hai tiêu đề và hai nhãn nút khác nhau**.

Trạng thái: chưa triển khai (spec chốt 2026-09-04; nhánh dự kiến `phase15-parent-zone`).
